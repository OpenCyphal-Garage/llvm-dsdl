# Leading and trailing underscores survive DSDL validation (only `_name_` is reserved) but are
# dropped by the snake_case projection, so `_foo` and `foo_` fold together.
uint8 _foo
uint8 foo_
uint8 __bar
@sealed
