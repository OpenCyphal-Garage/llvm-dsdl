\* TLC configuration for CyphalSerdes.
\* Bounds are intentionally small; this is exhaustive over the bounded domain.
\* Crank ScalarVals / MaxElems (and TestTypes in the module) to widen coverage.

CONSTANTS
    ScalarVals = {0, 1}
    MaxElems   = 2

SPECIFICATION Spec

INVARIANT Inv
