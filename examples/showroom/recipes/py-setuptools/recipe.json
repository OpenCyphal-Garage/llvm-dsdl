{
  "name": "py-setuptools",
  "title": "setuptools",
  "row": "Python",
  "language": "Python",
  "build_system": "setuptools",
  "idiom": "A",
  "dep_strategy": "none",
  "summary": "Needs no `pyproject.toml` of its own: dsdlc writes one declaring setuptools, so the generated tree is already an installable package and the host installs it.",
  "notes": "Only this Python backend gets Idiom A: it is the backend dsdlc's emitted manifest declares. A project cannot have two build backends, so choosing any other one means owning the manifest yourself, as the other four recipes do.\n\nEvery Python recipe builds its own virtual environment and lets pip fetch the build backend into it. That is correct isolation regardless, and on a distribution that marks its interpreter externally managed (PEP 668) -- which the toolshed image does -- nothing else works.",
  "generated_dir": "generated",
  "requires": [
    {
      "command": "python3",
      "reason": "creates the virtual environment the backend installs into"
    }
  ],
  "steps": [
    {
      "name": "generate",
      "run": [
        "{dsdlc}",
        "--target-language",
        "python",
        "--versioned-type-names",
        "dsdl/lanyard",
        "--py-package",
        "lanyard",
        "--outdir",
        "generated"
      ]
    },
    {
      "name": "virtual environment",
      "run": [
        "{python}",
        "-m",
        "venv",
        ".venv"
      ]
    },
    {
      "name": "install",
      "run": [
        ".venv/bin/pip",
        "install",
        "-q",
        "./generated"
      ]
    },
    {
      "name": "round-trip",
      "run": [
        ".venv/bin/python",
        "src/python/test_roundtrip.py"
      ]
    }
  ]
}
