{
  "name": "py-uv-build",
  "title": "uv-build",
  "row": "Python",
  "language": "Python",
  "build_system": "uv-build",
  "idiom": "B",
  "dep_strategy": "none",
  "summary": "The host owns `pyproject.toml` and points uv's own build backend at the generated package with `module-root` and `module-name`.",
  "notes": "`uv_build` wants the package's parent directory as `module-root` and the package name as `module-name`, rather than a path to the package itself. Otherwise this is the hatchling recipe with a different backend block.\n\nEvery Python recipe builds its own virtual environment and lets pip fetch the build backend into it. That is correct isolation regardless, and it is the only thing that works on a distribution that marks its interpreter externally managed (PEP 668) -- which the toolshed image does.",
  "generated_dir": "generated",
  "requires": [
    {
      "command": "python3",
      "reason": "creates the virtual environment the backend installs into"
    }
  ],
  "steps": [
    {
      "name": "generate",
      "run": [
        "{dsdlc}",
        "--target-language",
        "python",
        "--versioned-type-names",
        "dsdl/lanyard",
        "--py-package",
        "lanyard",
        "--outdir",
        "generated"
      ]
    },
    {
      "name": "virtual environment",
      "run": [
        "{python}",
        "-m",
        "venv",
        ".venv"
      ]
    },
    {
      "name": "install",
      "run": [
        ".venv/bin/pip",
        "install",
        "-q",
        "."
      ]
    },
    {
      "name": "round-trip",
      "run": [
        ".venv/bin/python",
        "src/python/test_roundtrip.py"
      ]
    }
  ]
}
