{
  "name": "py-hatchling",
  "title": "hatchling",
  "row": "Python",
  "language": "Python",
  "build_system": "hatchling",
  "idiom": "B",
  "dep_strategy": "none",
  "summary": "The host owns `pyproject.toml` and points hatchling at the generated package with `[tool.hatch.build.targets.wheel] packages`.",
  "notes": "The generated tree is an ordinary Python package, so pointing a backend at it is one line of configuration. dsdlc's own `pyproject.toml` is still sitting in `generated/`, inert -- nothing builds that directory.\n\nEvery Python recipe builds its own virtual environment and lets pip fetch the build backend into it. That is correct isolation regardless, and it is the only thing that works on a distribution that marks its interpreter externally managed (PEP 668) -- which the toolshed image does.",
  "generated_dir": "generated",
  "requires": [
    {
      "command": "python3",
      "reason": "creates the virtual environment the backend installs into"
    }
  ],
  "steps": [
    {
      "name": "generate",
      "run": [
        "{dsdlc}",
        "--target-language",
        "python",
        "dsdl/lanyard",
        "--py-package",
        "lanyard",
        "--outdir",
        "generated"
      ]
    },
    {
      "name": "virtual environment",
      "run": [
        "{python}",
        "-m",
        "venv",
        ".venv"
      ]
    },
    {
      "name": "install",
      "run": [
        ".venv/bin/pip",
        "install",
        "-q",
        "."
      ]
    },
    {
      "name": "round-trip",
      "run": [
        ".venv/bin/python",
        "src/python/test_roundtrip.py"
      ]
    }
  ]
}
