# Build directories
build/

# IDE and editor files
.cache/
.idea/
*.swp
*.swo
*~
.DS_Store

# Compiled objects
*.o
*.obj
*.a
*.so
*.dylib

# CMake generated files
CMakeUserPresets.json

# coverage
*.profraw

# Python
__pycache__/
*.py[cod]
*.pyd
.python-version
.venv/
.venv-docs/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.nox/
.coverage
.coverage.*

#nnvg
nunavut_out

#dsdlc
dsdl_out

# MkDocs output
site/
.actrc

# Reference DSDL compiler checkouts for the differential parity lane (CI clones
# these; local runs may too). Provisioned, never committed.
.reference/

# TLA+ / TLC model-checker artifacts (spec/tla/check.sh runs TLC in spec/tla/).
# The .tla / .cfg sources and the vendored tla2tools.jar are committed; everything
# TLC generates is not.
/spec/tla/states/
/spec/tla/*.st
/spec/tla/.tlacache/
*_TTrace_*.tla
*_TTrace_*.bin
