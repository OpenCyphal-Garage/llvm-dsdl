// Generated by llvmdsdl 0.2.3 (TypeScript backend).
// Source: uavcan.time.GetSynchronizationMasterInfo.0.1
import * as dsdlRuntime from "../../dsdl_runtime";

import { deserializeTAIInfo, serializeTAIInfo } from "./tai_info_0_1";
import { deserializeTimeSystem, serializeTimeSystem } from "./time_system_0_1";
import type { TAIInfo } from "./tai_info_0_1";
import type { TimeSystem } from "./time_system_0_1";
export const LLVMDSDL_GENERATOR_VERSION = "0.2.3";
export const DSDL_FULL_NAME = "uavcan.time.GetSynchronizationMasterInfo";
export const DSDL_IS_DEPRECATED = false;
export const DSDL_VERSION_MAJOR = 0;
export const DSDL_VERSION_MINOR = 1;
export const DSDL_REQUEST_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_REQUEST_ZOH_ALIAS_REASON = "not-sealed";
export const DSDL_RESPONSE_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_RESPONSE_ZOH_ALIAS_REASON = "invalid-bit-length";

// Every node that acts as a time synchronization master, or is capable of acting as such,
// should support this service.
// Its objective is to provide information about which time system is currently used in the network.
//
// Once a time system is chosen, it cannot be changed as long as at least one node on the network is running.
// In other words, the time system cannot be changed while the network is operating.
// An implication of this is that if there are redundant time synchronization masters, they all shall
// use the same time system always.
export interface GetSynchronizationMasterInfo_Request {
}


export function serializeGetSynchronizationMasterInfo_Request(value: GetSynchronizationMasterInfo_Request): Uint8Array {
  const out = new Uint8Array(0);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_time_GetSynchronizationMasterInfo_0_1__request = (capacityBits: number): boolean => 0 <= capacityBits;

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_time_GetSynchronizationMasterInfo_0_1__request(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeGetSynchronizationMasterInfo_Request(bytes: Uint8Array): { value: GetSynchronizationMasterInfo_Request; consumed: number } {
  const value = {} as GetSynchronizationMasterInfo_Request;
  let offsetBits = 0;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}

// Every node that acts as a time synchronization master, or is capable of acting as such,
// should support this service.
// Its objective is to provide information about which time system is currently used in the network.
//
// Once a time system is chosen, it cannot be changed as long as at least one node on the network is running.
// In other words, the time system cannot be changed while the network is operating.
// An implication of this is that if there are redundant time synchronization masters, they all shall
// use the same time system always.
export interface GetSynchronizationMasterInfo_Response {
  // [second^2]
  // Error variance, in second^2, of the time value reported by this master.
  // This value is allowed to change freely while the master is running.
  // For example, if the master's own clock is synchronized with a GNSS, the error variance is expected to increase
  // as signal reception deteriorates. If the signal is lost, this value is expected to grow steadily, the rate of
  // growth would be dependent on the quality of the time keeping hardware available locally (bad hardware yields
  // faster growth). Once the signal is regained, this value would drop back to nominal.
  error_variance: number;
  // Time system currently in use by the master.
  // Cannot be changed while the network is operating.
  time_system: TimeSystem;
  // Actual information about TAI provided by this master, if supported.
  // The fields in this data type are optional.
  tai_info: TAIInfo;
}


export function serializeGetSynchronizationMasterInfo_Response(value: GetSynchronizationMasterInfo_Response): Uint8Array {
  const out = new Uint8Array(7);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_time_GetSynchronizationMasterInfo_0_1__response = (capacityBits: number): boolean => 56 <= capacityBits;
  const mlir_llvmdsdl_plan_scalar_float__uavcan_time_GetSynchronizationMasterInfo_0_1__response__0__ser = (value: number): number => value;

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_time_GetSynchronizationMasterInfo_0_1__response(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  dsdlRuntime.writeFloat(out, offsetBits, 32, mlir_llvmdsdl_plan_scalar_float__uavcan_time_GetSynchronizationMasterInfo_0_1__response__0__ser(value.error_variance));
  offsetBits += 32;
  const time_systemAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let time_systemAlignBit = offsetBits; time_systemAlignBit < time_systemAlignedOffsetBits; ++time_systemAlignBit) {
    dsdlRuntime.setBit(out, time_systemAlignBit, false);
  }
  offsetBits = time_systemAlignedOffsetBits;
  const time_systemBytes = serializeTimeSystem(value.time_system);
  dsdlRuntime.copyBits(out, offsetBits, time_systemBytes, 0, 8);
  offsetBits += 8;
  const tai_infoAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let tai_infoAlignBit = offsetBits; tai_infoAlignBit < tai_infoAlignedOffsetBits; ++tai_infoAlignBit) {
    dsdlRuntime.setBit(out, tai_infoAlignBit, false);
  }
  offsetBits = tai_infoAlignedOffsetBits;
  const tai_infoBytes = serializeTAIInfo(value.tai_info);
  dsdlRuntime.copyBits(out, offsetBits, tai_infoBytes, 0, 16);
  offsetBits += 16;
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeGetSynchronizationMasterInfo_Response(bytes: Uint8Array): { value: GetSynchronizationMasterInfo_Response; consumed: number } {
  const mlir_llvmdsdl_plan_scalar_float__uavcan_time_GetSynchronizationMasterInfo_0_1__response__0__deser = (value: number): number => value;

  const value = {} as GetSynchronizationMasterInfo_Response;
  let offsetBits = 0;
  const error_varianceRaw = dsdlRuntime.readFloat(bytes, offsetBits, 32);
  value.error_variance = mlir_llvmdsdl_plan_scalar_float__uavcan_time_GetSynchronizationMasterInfo_0_1__response__0__deser(error_varianceRaw);
  offsetBits += 32;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const time_systemBytes = dsdlRuntime.extractBits(bytes, offsetBits, 8);
  value.time_system = deserializeTimeSystem(time_systemBytes).value;
  offsetBits += 8;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const tai_infoBytes = dsdlRuntime.extractBits(bytes, offsetBits, 16);
  value.tai_info = deserializeTAIInfo(tai_infoBytes).value;
  offsetBits += 16;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}

export type GetSynchronizationMasterInfo = GetSynchronizationMasterInfo_Request;
