// Generated by llvmdsdl 0.2.3 (TypeScript backend).
// Source: uavcan.time.TimeSystem.0.1
import * as dsdlRuntime from "../../dsdl_runtime";

export const LLVMDSDL_GENERATOR_VERSION = "0.2.3";
export const DSDL_FULL_NAME = "uavcan.time.TimeSystem";
export const DSDL_IS_DEPRECATED = false;
export const DSDL_VERSION_MAJOR = 0;
export const DSDL_VERSION_MINOR = 1;
export const DSDL_REQUEST_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_REQUEST_ZOH_ALIAS_REASON = "sub-byte-field";
export const DSDL_RESPONSE_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_RESPONSE_ZOH_ALIAS_REASON = "not-applicable";

// Time system enumeration.
// The time system shall be the same for all masters in the network.
// It cannot be changed while the network is running.
export interface TimeSystem {
  value: number;
}

// Monotonic time since boot.
// Monotonic time is a time reference that doesn't change rate or make leaps.
export const TIME_SYSTEM_MONOTONIC_SINCE_BOOT = 0;
// International Atomic Time; https://en.wikipedia.org/wiki/International_Atomic_Time.
// The timestamp value contains the number of microseconds elapsed since 1970-01-01T00:00:00Z TAI.
// TAI is always a fixed integer number of seconds ahead of GPS time.
// Systems that use GPS time as a reference should convert that to TAI by adding the fixed difference.
// GPS time is not supported for reasons of consistency across different positioning systems and applications.
export const TIME_SYSTEM_TAI = 1;
// Application-specific time system of unknown properties.
export const TIME_SYSTEM_APPLICATION_SPECIFIC = 15;

export function serializeTimeSystem(value: TimeSystem): Uint8Array {
  const out = new Uint8Array(1);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_time_TimeSystem_0_1 = (capacityBits: number): boolean => 8 <= capacityBits;
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_time_TimeSystem_0_1__0__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 15n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_time_TimeSystem_0_1(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 4, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_time_TimeSystem_0_1__0__ser(value.value), false);
  offsetBits += 4;
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeTimeSystem(bytes: Uint8Array): { value: TimeSystem; consumed: number } {
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_time_TimeSystem_0_1__0__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 15n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };

  const value = {} as TimeSystem;
  let offsetBits = 0;
  const valueRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 4);
  value.value = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_time_TimeSystem_0_1__0__deser(valueRaw));
  offsetBits += 4;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}
