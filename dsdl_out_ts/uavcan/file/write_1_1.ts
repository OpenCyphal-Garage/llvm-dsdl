// Generated by llvmdsdl 0.2.3 (TypeScript backend).
// Source: uavcan.file.Write.1.1
import * as dsdlRuntime from "../../dsdl_runtime";

import { deserializeUnstructured, serializeUnstructured } from "../primitive/unstructured_1_0";
import { deserializeError, serializeError } from "./error_1_0";
import { deserializePath, serializePath } from "./path_2_0";
import type { Unstructured } from "../primitive/unstructured_1_0";
import type { Error } from "./error_1_0";
import type { Path } from "./path_2_0";
export const LLVMDSDL_GENERATOR_VERSION = "0.2.3";
export const DSDL_FULL_NAME = "uavcan.file.Write";
export const DSDL_IS_DEPRECATED = false;
export const DSDL_VERSION_MAJOR = 1;
export const DSDL_VERSION_MINOR = 1;
export const DSDL_REQUEST_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_REQUEST_ZOH_ALIAS_REASON = "invalid-bit-length";
export const DSDL_RESPONSE_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_RESPONSE_ZOH_ALIAS_REASON = "invalid-bit-length";

// Write into a remote file.
// The server shall place the contents of the field 'data' into the file pointed by 'path' at the offset specified by
// the field 'offset'.
//
// When writing a file, the client should repeatedly call this service with data while advancing the offset until the
// file is written completely. When the write sequence is completed, the client shall call the service one last time,
// with the offset set to the size of the file and with the data field empty, which will signal the server that the
// transfer is finished.
//
// When the write operation is complete, the server shall truncate the resulting file past the specified offset.
export interface Write_Request {
  offset: number;
  path: Path;
  data: Unstructured;
}


export function serializeWrite_Request(value: Write_Request): Uint8Array {
  const out = new Uint8Array(519);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_file_Write_1_1__request = (capacityBits: number): boolean => 4152 <= capacityBits;
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_file_Write_1_1__request__0__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 1099511627775n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_file_Write_1_1__request(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 40, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_file_Write_1_1__request__0__ser(value.offset), false);
  offsetBits += 40;
  const pathAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let pathAlignBit = offsetBits; pathAlignBit < pathAlignedOffsetBits; ++pathAlignBit) {
    dsdlRuntime.setBit(out, pathAlignBit, false);
  }
  offsetBits = pathAlignedOffsetBits;
  const pathBytes = serializePath(value.path);
  dsdlRuntime.copyBits(out, offsetBits, pathBytes, 0, 2048);
  offsetBits += 2048;
  const dataAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let dataAlignBit = offsetBits; dataAlignBit < dataAlignedOffsetBits; ++dataAlignBit) {
    dsdlRuntime.setBit(out, dataAlignBit, false);
  }
  offsetBits = dataAlignedOffsetBits;
  const dataBytes = serializeUnstructured(value.data);
  dsdlRuntime.copyBits(out, offsetBits, dataBytes, 0, 2064);
  offsetBits += 2064;
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeWrite_Request(bytes: Uint8Array): { value: Write_Request; consumed: number } {
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_file_Write_1_1__request__0__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 1099511627775n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };

  const value = {} as Write_Request;
  let offsetBits = 0;
  const offsetRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 40);
  value.offset = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_file_Write_1_1__request__0__deser(offsetRaw));
  offsetBits += 40;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const pathBytes = dsdlRuntime.extractBits(bytes, offsetBits, 2048);
  value.path = deserializePath(pathBytes).value;
  offsetBits += 2048;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const dataBytes = dsdlRuntime.extractBits(bytes, offsetBits, 2064);
  value.data = deserializeUnstructured(dataBytes).value;
  offsetBits += 2064;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}

// Write into a remote file.
// The server shall place the contents of the field 'data' into the file pointed by 'path' at the offset specified by
// the field 'offset'.
//
// When writing a file, the client should repeatedly call this service with data while advancing the offset until the
// file is written completely. When the write sequence is completed, the client shall call the service one last time,
// with the offset set to the size of the file and with the data field empty, which will signal the server that the
// transfer is finished.
//
// When the write operation is complete, the server shall truncate the resulting file past the specified offset.
export interface Write_Response {
  error: Error;
}


export function serializeWrite_Response(value: Write_Response): Uint8Array {
  const out = new Uint8Array(2);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_file_Write_1_1__response = (capacityBits: number): boolean => 16 <= capacityBits;

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_file_Write_1_1__response(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  const errorAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let errorAlignBit = offsetBits; errorAlignBit < errorAlignedOffsetBits; ++errorAlignBit) {
    dsdlRuntime.setBit(out, errorAlignBit, false);
  }
  offsetBits = errorAlignedOffsetBits;
  const errorBytes = serializeError(value.error);
  dsdlRuntime.copyBits(out, offsetBits, errorBytes, 0, 16);
  offsetBits += 16;
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeWrite_Response(bytes: Uint8Array): { value: Write_Response; consumed: number } {
  const value = {} as Write_Response;
  let offsetBits = 0;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const errorBytes = dsdlRuntime.extractBits(bytes, offsetBits, 16);
  value.error = deserializeError(errorBytes).value;
  offsetBits += 16;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}

export type Write = Write_Request;
