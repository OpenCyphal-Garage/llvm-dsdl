// Generated by llvmdsdl 0.2.3 (TypeScript backend).
// Source: uavcan.file.Path.2.0
import * as dsdlRuntime from "../../dsdl_runtime";

export const LLVMDSDL_GENERATOR_VERSION = "0.2.3";
export const DSDL_FULL_NAME = "uavcan.file.Path";
export const DSDL_IS_DEPRECATED = false;
export const DSDL_VERSION_MAJOR = 2;
export const DSDL_VERSION_MINOR = 0;
export const DSDL_REQUEST_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_REQUEST_ZOH_ALIAS_REASON = "variable-array";
export const DSDL_RESPONSE_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_RESPONSE_ZOH_ALIAS_REASON = "not-applicable";

// Nested type.
// A file system path encoded in UTF8. The only valid separator is the forward slash "/".
// A single slash ("/") refers to the root directory (the location of which is defined by the server).
// Relative references (e.g. "..") are not defined and not permitted (although this may change in the future).
// Conventions (not enforced):
//   - A path pointing to a file or a link to file should not end with a separator.
//   - A path pointing to a directory or to a link to directory should end with a separator.
export interface Path {
  path: Array<number>;
}

export const PATH_SEPARATOR = "/";
export const PATH_MAX_LENGTH = 255;

export function serializePath(value: Path): Uint8Array {
  const out = new Uint8Array(256);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_file_Path_2_0 = (capacityBits: number): boolean => 2048 <= capacityBits;
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_file_Path_2_0__0__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 255n ? 255n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };
  const mlir_llvmdsdl_plan_array_length_prefix__uavcan_file_Path_2_0__0__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_validate_array_length__uavcan_file_Path_2_0__0 = (value: number): boolean => (value >= 0) && (value <= 255);

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_file_Path_2_0(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  const pathArray = value.path;
  if (!Array.isArray(pathArray)) {
    throw new Error("field 'path' expects an array");
  }
  if (!mlir_llvmdsdl_plan_validate_array_length__uavcan_file_Path_2_0__0(pathArray.length)) {
    throw new Error("field 'path' exceeds max length 255");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 8, Number(mlir_llvmdsdl_plan_array_length_prefix__uavcan_file_Path_2_0__0__ser(pathArray.length)), false);
  offsetBits += 8;
  for (let i = 0; i < pathArray.length; ++i) {
    dsdlRuntime.writeUnsigned(out, offsetBits, 8, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_file_Path_2_0__0__ser(pathArray[i]), true);
    offsetBits += 8;
  }
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializePath(bytes: Uint8Array): { value: Path; consumed: number } {
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_file_Path_2_0__0__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_array_length_prefix__uavcan_file_Path_2_0__0__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_validate_array_length__uavcan_file_Path_2_0__0 = (value: number): boolean => (value >= 0) && (value <= 255);

  const value = {} as Path;
  let offsetBits = 0;
  const pathLengthRaw = Math.trunc(dsdlRuntime.readUnsigned(bytes, offsetBits, 8));
  offsetBits += 8;
  const pathLength = Number(mlir_llvmdsdl_plan_array_length_prefix__uavcan_file_Path_2_0__0__deser(pathLengthRaw));
  if (!mlir_llvmdsdl_plan_validate_array_length__uavcan_file_Path_2_0__0(pathLength)) {
    throw new Error("decoded length for field 'path' exceeds max length 255");
  }
  const pathArray: Array<number> = new Array(pathLength);
  for (let i = 0; i < pathLength; ++i) {
    const pathItemRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 8);
    pathArray[i] = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_file_Path_2_0__0__deser(pathItemRaw));
    offsetBits += 8;
  }
  value.path = pathArray;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}
