// Generated by llvmdsdl 0.2.3 (TypeScript backend).
// Source: uavcan.file.Read.1.1
import * as dsdlRuntime from "../../dsdl_runtime";

import { deserializeUnstructured, serializeUnstructured } from "../primitive/unstructured_1_0";
import { deserializeError, serializeError } from "./error_1_0";
import { deserializePath, serializePath } from "./path_2_0";
import type { Unstructured } from "../primitive/unstructured_1_0";
import type { Error } from "./error_1_0";
import type { Path } from "./path_2_0";
export const LLVMDSDL_GENERATOR_VERSION = "0.2.3";
export const DSDL_FULL_NAME = "uavcan.file.Read";
export const DSDL_IS_DEPRECATED = false;
export const DSDL_VERSION_MAJOR = 1;
export const DSDL_VERSION_MINOR = 1;
export const DSDL_REQUEST_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_REQUEST_ZOH_ALIAS_REASON = "invalid-bit-length";
export const DSDL_RESPONSE_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_RESPONSE_ZOH_ALIAS_REASON = "invalid-bit-length";

// Read file from a remote node.
//
// There are two possible outcomes of a successful call:
//  1. Data array size equals its capacity. This means that the end of the file is not reached yet.
//  2. Data array size is less than its capacity, possibly zero. This means that the end of the file is reached.
//
// Thus, if the client needs to fetch the entire file, it should repeatedly call this service while increasing the
// offset, until a non-full data array is returned.
//
// If the object pointed by 'path' cannot be read (e.g. it is a directory or it does not exist), an appropriate error
// code will be returned, and the data array will be empty.
//
// It is easy to see that this protocol is prone to race conditions because the remote file can be modified
// between read operations which might result in the client obtaining a damaged file. To combat this,
// application designers are recommended to adhere to the following convention. Let every file whose integrity
// is of interest have a hash or a digital signature, which is stored in an adjacent file under the same name
// suffixed with the appropriate extension according to the type of hash or digital signature used.
// For example, let there be file "image.bin", integrity of which shall be ensured by the client upon downloading.
// Suppose that the file is hashed using SHA-256, so the appropriate file extension for the hash would be
// ".sha256". Following this convention, the hash of "image.bin" would be stored in "image.bin.sha256".
// After downloading the file, the client would read the hash (being small, the hash can be read in a single
// request) and check it against a locally computed value. Some servers may opt to generate such hash files
// automatically as necessary; for example, if such file is requested but it does not exist, the server would
// compute the necessary signature or hash (the type of hash/signature can be deduced from the requested file
// extension) and return it as if the file existed. Obviously, this would be impractical for very large files;
// in that case, hash/signature should be pre-computed and stored in a real file. If this approach is followed,
// implementers are advised to use only SHA-256 for hashing, in order to reduce the number of fielded
// incompatible implementations.
export interface Read_Request {
  offset: number;
  path: Path;
}


export function serializeRead_Request(value: Read_Request): Uint8Array {
  const out = new Uint8Array(261);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_file_Read_1_1__request = (capacityBits: number): boolean => 2088 <= capacityBits;
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_file_Read_1_1__request__0__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 1099511627775n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_file_Read_1_1__request(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 40, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_file_Read_1_1__request__0__ser(value.offset), false);
  offsetBits += 40;
  const pathAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let pathAlignBit = offsetBits; pathAlignBit < pathAlignedOffsetBits; ++pathAlignBit) {
    dsdlRuntime.setBit(out, pathAlignBit, false);
  }
  offsetBits = pathAlignedOffsetBits;
  const pathBytes = serializePath(value.path);
  dsdlRuntime.copyBits(out, offsetBits, pathBytes, 0, 2048);
  offsetBits += 2048;
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeRead_Request(bytes: Uint8Array): { value: Read_Request; consumed: number } {
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_file_Read_1_1__request__0__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 1099511627775n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };

  const value = {} as Read_Request;
  let offsetBits = 0;
  const offsetRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 40);
  value.offset = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_file_Read_1_1__request__0__deser(offsetRaw));
  offsetBits += 40;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const pathBytes = dsdlRuntime.extractBits(bytes, offsetBits, 2048);
  value.path = deserializePath(pathBytes).value;
  offsetBits += 2048;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}

// Read file from a remote node.
//
// There are two possible outcomes of a successful call:
//  1. Data array size equals its capacity. This means that the end of the file is not reached yet.
//  2. Data array size is less than its capacity, possibly zero. This means that the end of the file is reached.
//
// Thus, if the client needs to fetch the entire file, it should repeatedly call this service while increasing the
// offset, until a non-full data array is returned.
//
// If the object pointed by 'path' cannot be read (e.g. it is a directory or it does not exist), an appropriate error
// code will be returned, and the data array will be empty.
//
// It is easy to see that this protocol is prone to race conditions because the remote file can be modified
// between read operations which might result in the client obtaining a damaged file. To combat this,
// application designers are recommended to adhere to the following convention. Let every file whose integrity
// is of interest have a hash or a digital signature, which is stored in an adjacent file under the same name
// suffixed with the appropriate extension according to the type of hash or digital signature used.
// For example, let there be file "image.bin", integrity of which shall be ensured by the client upon downloading.
// Suppose that the file is hashed using SHA-256, so the appropriate file extension for the hash would be
// ".sha256". Following this convention, the hash of "image.bin" would be stored in "image.bin.sha256".
// After downloading the file, the client would read the hash (being small, the hash can be read in a single
// request) and check it against a locally computed value. Some servers may opt to generate such hash files
// automatically as necessary; for example, if such file is requested but it does not exist, the server would
// compute the necessary signature or hash (the type of hash/signature can be deduced from the requested file
// extension) and return it as if the file existed. Obviously, this would be impractical for very large files;
// in that case, hash/signature should be pre-computed and stored in a real file. If this approach is followed,
// implementers are advised to use only SHA-256 for hashing, in order to reduce the number of fielded
// incompatible implementations.
export interface Read_Response {
  error: Error;
  data: Unstructured;
}


export function serializeRead_Response(value: Read_Response): Uint8Array {
  const out = new Uint8Array(260);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_file_Read_1_1__response = (capacityBits: number): boolean => 2080 <= capacityBits;

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_file_Read_1_1__response(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  const errorAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let errorAlignBit = offsetBits; errorAlignBit < errorAlignedOffsetBits; ++errorAlignBit) {
    dsdlRuntime.setBit(out, errorAlignBit, false);
  }
  offsetBits = errorAlignedOffsetBits;
  const errorBytes = serializeError(value.error);
  dsdlRuntime.copyBits(out, offsetBits, errorBytes, 0, 16);
  offsetBits += 16;
  const dataAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let dataAlignBit = offsetBits; dataAlignBit < dataAlignedOffsetBits; ++dataAlignBit) {
    dsdlRuntime.setBit(out, dataAlignBit, false);
  }
  offsetBits = dataAlignedOffsetBits;
  const dataBytes = serializeUnstructured(value.data);
  dsdlRuntime.copyBits(out, offsetBits, dataBytes, 0, 2064);
  offsetBits += 2064;
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeRead_Response(bytes: Uint8Array): { value: Read_Response; consumed: number } {
  const value = {} as Read_Response;
  let offsetBits = 0;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const errorBytes = dsdlRuntime.extractBits(bytes, offsetBits, 16);
  value.error = deserializeError(errorBytes).value;
  offsetBits += 16;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const dataBytes = dsdlRuntime.extractBits(bytes, offsetBits, 2064);
  value.data = deserializeUnstructured(dataBytes).value;
  offsetBits += 2064;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}

export type Read = Read_Request;
