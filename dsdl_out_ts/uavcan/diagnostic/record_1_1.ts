// Generated by llvmdsdl 0.2.3 (TypeScript backend).
// Source: uavcan.diagnostic.Record.1.1
import * as dsdlRuntime from "../../dsdl_runtime";

import { deserializeSynchronizedTimestamp, serializeSynchronizedTimestamp } from "../time/synchronized_timestamp_1_0";
import { deserializeSeverity, serializeSeverity } from "./severity_1_0";
import type { SynchronizedTimestamp } from "../time/synchronized_timestamp_1_0";
import type { Severity } from "./severity_1_0";
export const LLVMDSDL_GENERATOR_VERSION = "0.2.3";
export const DSDL_FULL_NAME = "uavcan.diagnostic.Record";
export const DSDL_IS_DEPRECATED = false;
export const DSDL_VERSION_MAJOR = 1;
export const DSDL_VERSION_MINOR = 1;
export const DSDL_REQUEST_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_REQUEST_ZOH_ALIAS_REASON = "invalid-bit-length";
export const DSDL_RESPONSE_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_RESPONSE_ZOH_ALIAS_REASON = "not-applicable";

// Generic human-readable text message for logging and displaying purposes.
// Generally, it should be published at the lowest priority level.
export interface Record {
  // Optional timestamp in the network-synchronized time system; zero if undefined.
  // The timestamp value conveys the exact moment when the reported event took place.
  timestamp: SynchronizedTimestamp;
  severity: Severity;
  // Message text.
  // Normally, messages should be kept as short as possible, especially those of high severity.
  text: Array<number>;
}


export function serializeRecord(value: Record): Uint8Array {
  const out = new Uint8Array(264);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_diagnostic_Record_1_1 = (capacityBits: number): boolean => 2112 <= capacityBits;
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_diagnostic_Record_1_1__4__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 255n ? 255n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };
  const mlir_llvmdsdl_plan_array_length_prefix__uavcan_diagnostic_Record_1_1__4__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_validate_array_length__uavcan_diagnostic_Record_1_1__4 = (value: number): boolean => (value >= 0) && (value <= 255);

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_diagnostic_Record_1_1(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  const timestampAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let timestampAlignBit = offsetBits; timestampAlignBit < timestampAlignedOffsetBits; ++timestampAlignBit) {
    dsdlRuntime.setBit(out, timestampAlignBit, false);
  }
  offsetBits = timestampAlignedOffsetBits;
  const timestampBytes = serializeSynchronizedTimestamp(value.timestamp);
  dsdlRuntime.copyBits(out, offsetBits, timestampBytes, 0, 56);
  offsetBits += 56;
  const severityAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let severityAlignBit = offsetBits; severityAlignBit < severityAlignedOffsetBits; ++severityAlignBit) {
    dsdlRuntime.setBit(out, severityAlignBit, false);
  }
  offsetBits = severityAlignedOffsetBits;
  const severityBytes = serializeSeverity(value.severity);
  dsdlRuntime.copyBits(out, offsetBits, severityBytes, 0, 8);
  offsetBits += 8;
  const textArray = value.text;
  if (!Array.isArray(textArray)) {
    throw new Error("field 'text' expects an array");
  }
  if (!mlir_llvmdsdl_plan_validate_array_length__uavcan_diagnostic_Record_1_1__4(textArray.length)) {
    throw new Error("field 'text' exceeds max length 255");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 8, Number(mlir_llvmdsdl_plan_array_length_prefix__uavcan_diagnostic_Record_1_1__4__ser(textArray.length)), false);
  offsetBits += 8;
  for (let i = 0; i < textArray.length; ++i) {
    dsdlRuntime.writeUnsigned(out, offsetBits, 8, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_diagnostic_Record_1_1__4__ser(textArray[i]), true);
    offsetBits += 8;
  }
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeRecord(bytes: Uint8Array): { value: Record; consumed: number } {
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_diagnostic_Record_1_1__4__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_array_length_prefix__uavcan_diagnostic_Record_1_1__4__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_validate_array_length__uavcan_diagnostic_Record_1_1__4 = (value: number): boolean => (value >= 0) && (value <= 255);

  const value = {} as Record;
  let offsetBits = 0;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const timestampBytes = dsdlRuntime.extractBits(bytes, offsetBits, 56);
  value.timestamp = deserializeSynchronizedTimestamp(timestampBytes).value;
  offsetBits += 56;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const severityBytes = dsdlRuntime.extractBits(bytes, offsetBits, 8);
  value.severity = deserializeSeverity(severityBytes).value;
  offsetBits += 8;
  const textLengthRaw = Math.trunc(dsdlRuntime.readUnsigned(bytes, offsetBits, 8));
  offsetBits += 8;
  const textLength = Number(mlir_llvmdsdl_plan_array_length_prefix__uavcan_diagnostic_Record_1_1__4__deser(textLengthRaw));
  if (!mlir_llvmdsdl_plan_validate_array_length__uavcan_diagnostic_Record_1_1__4(textLength)) {
    throw new Error("decoded length for field 'text' exceeds max length 255");
  }
  const textArray: Array<number> = new Array(textLength);
  for (let i = 0; i < textLength; ++i) {
    const textItemRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 8);
    textArray[i] = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_diagnostic_Record_1_1__4__deser(textItemRaw));
    offsetBits += 8;
  }
  value.text = textArray;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}
