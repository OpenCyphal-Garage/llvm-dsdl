// Generated by llvmdsdl 0.2.3 (TypeScript backend).
// Source: uavcan.diagnostic.Severity.1.0
import * as dsdlRuntime from "../../dsdl_runtime";

export const LLVMDSDL_GENERATOR_VERSION = "0.2.3";
export const DSDL_FULL_NAME = "uavcan.diagnostic.Severity";
export const DSDL_IS_DEPRECATED = false;
export const DSDL_VERSION_MAJOR = 1;
export const DSDL_VERSION_MINOR = 0;
export const DSDL_REQUEST_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_REQUEST_ZOH_ALIAS_REASON = "sub-byte-field";
export const DSDL_RESPONSE_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_RESPONSE_ZOH_ALIAS_REASON = "not-applicable";

// Generic message severity representation.
export interface Severity {
  // The severity level ranging from 0 to 7, where low values represent low-severity (unimportant) messages, and
  // high values represent high-severity (important) messages. Several mnemonics for the severity levels are
  // defined below. Nodes are advised to implement output filtering mechanisms, allowing users to select
  // the minimal severity for emitted messages; messages of the selected and higher severity levels will
  // be published, and messages of lower severity will be suppressed (discarded).
  value: number;
}

// Messages of this severity can be used only during development.
// They shall not be used in a fielded operational system.
export const SEVERITY_TRACE = 0;
// Messages that can aid in troubleshooting.
// Messages of this severity and lower should be disabled by default.
export const SEVERITY_DEBUG = 1;
// General informational messages of low importance.
// Messages of this severity and lower should be disabled by default.
export const SEVERITY_INFO = 2;
// General informational messages of high importance.
// Messages of this severity and lower should be disabled by default.
export const SEVERITY_NOTICE = 3;
// Messages reporting abnormalities and warning conditions.
// Messages of this severity and higher should be enabled by default.
export const SEVERITY_WARNING = 4;
// Messages reporting problems and error conditions.
// Messages of this severity and higher should be enabled by default.
export const SEVERITY_ERROR = 5;
// Messages reporting serious problems and critical conditions.
// Messages of this severity and higher should be always enabled.
export const SEVERITY_CRITICAL = 6;
// Notifications of dangerous circumstances that demand immediate attention.
// Messages of this severity should be always enabled.
export const SEVERITY_ALERT = 7;

export function serializeSeverity(value: Severity): Uint8Array {
  const out = new Uint8Array(1);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_diagnostic_Severity_1_0 = (capacityBits: number): boolean => 8 <= capacityBits;
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_diagnostic_Severity_1_0__0__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 7n ? 7n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_diagnostic_Severity_1_0(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 3, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_diagnostic_Severity_1_0__0__ser(value.value), true);
  offsetBits += 3;
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeSeverity(bytes: Uint8Array): { value: Severity; consumed: number } {
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_diagnostic_Severity_1_0__0__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 7n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };

  const value = {} as Severity;
  let offsetBits = 0;
  const valueRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 3);
  value.value = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_diagnostic_Severity_1_0__0__deser(valueRaw));
  offsetBits += 3;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}
