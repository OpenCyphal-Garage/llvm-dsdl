// Generated by llvmdsdl 0.2.3 (TypeScript backend).
// Source: uavcan.internet.udp.HandleIncomingPacket.0.2
import * as dsdlRuntime from "../../../dsdl_runtime";

export const LLVMDSDL_GENERATOR_VERSION = "0.2.3";
export const DSDL_FULL_NAME = "uavcan.internet.udp.HandleIncomingPacket";
export const DSDL_IS_DEPRECATED = false;
export const DSDL_VERSION_MAJOR = 0;
export const DSDL_VERSION_MINOR = 2;
export const DSDL_REQUEST_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_REQUEST_ZOH_ALIAS_REASON = "variable-array";
export const DSDL_RESPONSE_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_RESPONSE_ZOH_ALIAS_REASON = "not-sealed";

// This message carries UDP packets sent from a remote host on the Internet or a LAN to a node on the local Cyphal bus.
// Please refer to the definition of the message type OutgoingPacket for a general overview of the packet forwarding
// logic.
//
// This data type has been made a service type rather than a message type in order to make its transfers addressable,
// allowing nodes to employ hardware acceptance filters for filtering out forwarded datagrams that are not addressed
// to them. Additionally, requiring the destination nodes to always respond upon reception of the forwarded datagram
// opens interesting opportunities for future extensions of the forwarding protocol. If the service invocation times
// out, the modem node is permitted to remove the corresponding entry from the NAT table immediately, not waiting
// for its TTL to expire.
//
// It should be noted that this data type definition intentionally leaves out the source address. This is done in
// order to simplify the implementation, reduce the bus traffic overhead, and because the nature of the
// communication patterns proposed by this set of messages does not provide a valid way to implement server hosts
// on the local Cyphal bus. It is assumed that local nodes can be only clients, and therefore, they will be able to
// determine the address of the sender simply by mapping the field session_id to their internally maintained states.
// Furthermore, it is uncertain what is the optimal way of representing the source address for
// client nodes: it is assumed that the local nodes will mostly use DNS names rather than IP addresses, so if there
// was a source address field, modem nodes would have to perform reverse mapping from the IP address they received
// the datagram from to the corresponding DNS name that was used by the local node with the outgoing message. This
// approach creates a number of troubling corner cases and adds a fair amount of hidden complexities to the
// implementation of modem nodes.
//
// It is recommended to perform service invocations at the same transfer priority level as was used for broadcasting
// the latest matching message of type OutgoingPacket. However, meeting this recommendation would require the modem
// node to implement additional logic, which may be undesirable. Therefore, implementers are free to deviate from
// this recommendation and resort to a fixed priority level instead. In the case of a fixed priority level, it is
// advised to use the lowest transfer priority level.
export interface HandleIncomingPacket_Request {
  // This field shall contain the same value that was used by the local node when sending the corresponding outgoing
  // packet using the message type OutgoingPacket. This value will be used by the local node to match the response
  // with its local context.
  session_id: number;
  // Effective payload. This data will be forwarded from the remote host verbatim.
  // UDP packets that contain more than 508 bytes of payload may be dropped by some types of
  // communication equipment. Refer to RFC 791 and 2460 for an in-depth review.
  // Datagrams that exceed the capacity of this field should be discarded by the modem node.
  payload: Array<number>;
}


export function serializeHandleIncomingPacket_Request(value: HandleIncomingPacket_Request): Uint8Array {
  const out = new Uint8Array(512);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_internet_udp_HandleIncomingPacket_0_2__request = (capacityBits: number): boolean => 4096 <= capacityBits;
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_internet_udp_HandleIncomingPacket_0_2__request__0__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 65535n ? 65535n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_internet_udp_HandleIncomingPacket_0_2__request__1__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 255n ? 255n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };
  const mlir_llvmdsdl_plan_array_length_prefix__uavcan_internet_udp_HandleIncomingPacket_0_2__request__1__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 65535n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_validate_array_length__uavcan_internet_udp_HandleIncomingPacket_0_2__request__1 = (value: number): boolean => (value >= 0) && (value <= 508);

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_internet_udp_HandleIncomingPacket_0_2__request(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 16, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_internet_udp_HandleIncomingPacket_0_2__request__0__ser(value.session_id), true);
  offsetBits += 16;
  const payloadArray = value.payload;
  if (!Array.isArray(payloadArray)) {
    throw new Error("field 'payload' expects an array");
  }
  if (!mlir_llvmdsdl_plan_validate_array_length__uavcan_internet_udp_HandleIncomingPacket_0_2__request__1(payloadArray.length)) {
    throw new Error("field 'payload' exceeds max length 508");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 16, Number(mlir_llvmdsdl_plan_array_length_prefix__uavcan_internet_udp_HandleIncomingPacket_0_2__request__1__ser(payloadArray.length)), false);
  offsetBits += 16;
  for (let i = 0; i < payloadArray.length; ++i) {
    dsdlRuntime.writeUnsigned(out, offsetBits, 8, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_internet_udp_HandleIncomingPacket_0_2__request__1__ser(payloadArray[i]), true);
    offsetBits += 8;
  }
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeHandleIncomingPacket_Request(bytes: Uint8Array): { value: HandleIncomingPacket_Request; consumed: number } {
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_internet_udp_HandleIncomingPacket_0_2__request__0__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 65535n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_internet_udp_HandleIncomingPacket_0_2__request__1__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_array_length_prefix__uavcan_internet_udp_HandleIncomingPacket_0_2__request__1__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 65535n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_validate_array_length__uavcan_internet_udp_HandleIncomingPacket_0_2__request__1 = (value: number): boolean => (value >= 0) && (value <= 508);

  const value = {} as HandleIncomingPacket_Request;
  let offsetBits = 0;
  const session_idRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 16);
  value.session_id = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_internet_udp_HandleIncomingPacket_0_2__request__0__deser(session_idRaw));
  offsetBits += 16;
  const payloadLengthRaw = Math.trunc(dsdlRuntime.readUnsigned(bytes, offsetBits, 16));
  offsetBits += 16;
  const payloadLength = Number(mlir_llvmdsdl_plan_array_length_prefix__uavcan_internet_udp_HandleIncomingPacket_0_2__request__1__deser(payloadLengthRaw));
  if (!mlir_llvmdsdl_plan_validate_array_length__uavcan_internet_udp_HandleIncomingPacket_0_2__request__1(payloadLength)) {
    throw new Error("decoded length for field 'payload' exceeds max length 508");
  }
  const payloadArray: Array<number> = new Array(payloadLength);
  for (let i = 0; i < payloadLength; ++i) {
    const payloadItemRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 8);
    payloadArray[i] = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_internet_udp_HandleIncomingPacket_0_2__request__1__deser(payloadItemRaw));
    offsetBits += 8;
  }
  value.payload = payloadArray;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}

// This message carries UDP packets sent from a remote host on the Internet or a LAN to a node on the local Cyphal bus.
// Please refer to the definition of the message type OutgoingPacket for a general overview of the packet forwarding
// logic.
//
// This data type has been made a service type rather than a message type in order to make its transfers addressable,
// allowing nodes to employ hardware acceptance filters for filtering out forwarded datagrams that are not addressed
// to them. Additionally, requiring the destination nodes to always respond upon reception of the forwarded datagram
// opens interesting opportunities for future extensions of the forwarding protocol. If the service invocation times
// out, the modem node is permitted to remove the corresponding entry from the NAT table immediately, not waiting
// for its TTL to expire.
//
// It should be noted that this data type definition intentionally leaves out the source address. This is done in
// order to simplify the implementation, reduce the bus traffic overhead, and because the nature of the
// communication patterns proposed by this set of messages does not provide a valid way to implement server hosts
// on the local Cyphal bus. It is assumed that local nodes can be only clients, and therefore, they will be able to
// determine the address of the sender simply by mapping the field session_id to their internally maintained states.
// Furthermore, it is uncertain what is the optimal way of representing the source address for
// client nodes: it is assumed that the local nodes will mostly use DNS names rather than IP addresses, so if there
// was a source address field, modem nodes would have to perform reverse mapping from the IP address they received
// the datagram from to the corresponding DNS name that was used by the local node with the outgoing message. This
// approach creates a number of troubling corner cases and adds a fair amount of hidden complexities to the
// implementation of modem nodes.
//
// It is recommended to perform service invocations at the same transfer priority level as was used for broadcasting
// the latest matching message of type OutgoingPacket. However, meeting this recommendation would require the modem
// node to implement additional logic, which may be undesirable. Therefore, implementers are free to deviate from
// this recommendation and resort to a fixed priority level instead. In the case of a fixed priority level, it is
// advised to use the lowest transfer priority level.
export interface HandleIncomingPacket_Response {
}


export function serializeHandleIncomingPacket_Response(value: HandleIncomingPacket_Response): Uint8Array {
  const out = new Uint8Array(0);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_internet_udp_HandleIncomingPacket_0_2__response = (capacityBits: number): boolean => 0 <= capacityBits;

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_internet_udp_HandleIncomingPacket_0_2__response(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeHandleIncomingPacket_Response(bytes: Uint8Array): { value: HandleIncomingPacket_Response; consumed: number } {
  const value = {} as HandleIncomingPacket_Response;
  let offsetBits = 0;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}

export type HandleIncomingPacket = HandleIncomingPacket_Request;
