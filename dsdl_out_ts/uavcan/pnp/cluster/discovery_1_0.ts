// Generated by llvmdsdl 0.2.3 (TypeScript backend).
// Source: uavcan.pnp.cluster.Discovery.1.0
import * as dsdlRuntime from "../../../dsdl_runtime";

import { deserializeID, serializeID } from "../../node/id_1_0";
import type { ID } from "../../node/id_1_0";
export const LLVMDSDL_GENERATOR_VERSION = "0.2.3";
export const DSDL_FULL_NAME = "uavcan.pnp.cluster.Discovery";
export const DSDL_IS_DEPRECATED = false;
export const DSDL_VERSION_MAJOR = 1;
export const DSDL_VERSION_MINOR = 0;
export const DSDL_REQUEST_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_REQUEST_ZOH_ALIAS_REASON = "sub-byte-field";
export const DSDL_RESPONSE_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_RESPONSE_ZOH_ALIAS_REASON = "not-applicable";

// This message is used by redundant allocators to find each other's node-ID.
// Please refer to the type AppendEntries for details.
//
// An allocator should stop publishing this message as soon as it has discovered all other allocators in the cluster.
//
// An exception applies: when an allocator receives a Discovery message where the list of known nodes is incomplete
// (i.e. len(known_nodes) < configured_cluster_size), it shall publish a Discovery message once. This condition
// allows other allocators to quickly re-discover the cluster after a restart.
export interface Discovery {
  // The number of allocators in the cluster as configured on the sender.
  // This value shall be the same across all allocators.
  configured_cluster_size: number;
  // Node-IDs of the allocators that are known to the publishing allocator, including the publishing allocator itself.
  known_nodes: Array<ID>;
}

// [second]
// This message should be broadcasted by the allocator at this interval until all other allocators are discovered.
export const DISCOVERY_BROADCASTING_PERIOD = 1;
// The redundant allocator cluster cannot contain more than 5 allocators.
export const DISCOVERY_MAX_CLUSTER_SIZE = 5;

export function serializeDiscovery(value: Discovery): Uint8Array {
  const out = new Uint8Array(12);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_pnp_cluster_Discovery_1_0 = (capacityBits: number): boolean => 96 <= capacityBits;
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_pnp_cluster_Discovery_1_0__0__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 7n ? 7n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };
  const mlir_llvmdsdl_plan_array_length_prefix__uavcan_pnp_cluster_Discovery_1_0__3__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_validate_array_length__uavcan_pnp_cluster_Discovery_1_0__3 = (value: number): boolean => (value >= 0) && (value <= 5);

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_pnp_cluster_Discovery_1_0(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 3, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_pnp_cluster_Discovery_1_0__0__ser(value.configured_cluster_size), true);
  offsetBits += 3;
  for (let _PaddingBit = 0; _PaddingBit < 5; ++_PaddingBit) {
    dsdlRuntime.setBit(out, offsetBits + _PaddingBit, false);
  }
  offsetBits += 5;
  const known_nodesAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let known_nodesAlignBit = offsetBits; known_nodesAlignBit < known_nodesAlignedOffsetBits; ++known_nodesAlignBit) {
    dsdlRuntime.setBit(out, known_nodesAlignBit, false);
  }
  offsetBits = known_nodesAlignedOffsetBits;
  const known_nodesArray = value.known_nodes;
  if (!Array.isArray(known_nodesArray)) {
    throw new Error("field 'known_nodes' expects an array");
  }
  if (!mlir_llvmdsdl_plan_validate_array_length__uavcan_pnp_cluster_Discovery_1_0__3(known_nodesArray.length)) {
    throw new Error("field 'known_nodes' exceeds max length 5");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 8, Number(mlir_llvmdsdl_plan_array_length_prefix__uavcan_pnp_cluster_Discovery_1_0__3__ser(known_nodesArray.length)), false);
  offsetBits += 8;
  for (let i = 0; i < known_nodesArray.length; ++i) {
    const known_nodesBytes = serializeID(known_nodesArray[i]);
    dsdlRuntime.copyBits(out, offsetBits, known_nodesBytes, 0, 16);
    offsetBits += 16;
  }
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeDiscovery(bytes: Uint8Array): { value: Discovery; consumed: number } {
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_pnp_cluster_Discovery_1_0__0__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 7n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_array_length_prefix__uavcan_pnp_cluster_Discovery_1_0__3__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_validate_array_length__uavcan_pnp_cluster_Discovery_1_0__3 = (value: number): boolean => (value >= 0) && (value <= 5);

  const value = {} as Discovery;
  let offsetBits = 0;
  const configured_cluster_sizeRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 3);
  value.configured_cluster_size = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_pnp_cluster_Discovery_1_0__0__deser(configured_cluster_sizeRaw));
  offsetBits += 3;
  offsetBits += 5;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const known_nodesLengthRaw = Math.trunc(dsdlRuntime.readUnsigned(bytes, offsetBits, 8));
  offsetBits += 8;
  const known_nodesLength = Number(mlir_llvmdsdl_plan_array_length_prefix__uavcan_pnp_cluster_Discovery_1_0__3__deser(known_nodesLengthRaw));
  if (!mlir_llvmdsdl_plan_validate_array_length__uavcan_pnp_cluster_Discovery_1_0__3(known_nodesLength)) {
    throw new Error("decoded length for field 'known_nodes' exceeds max length 5");
  }
  const known_nodesArray: Array<ID> = new Array(known_nodesLength);
  for (let i = 0; i < known_nodesLength; ++i) {
    const known_nodesBytes = dsdlRuntime.extractBits(bytes, offsetBits, 16);
    known_nodesArray[i] = deserializeID(known_nodesBytes).value;
    offsetBits += 16;
  }
  value.known_nodes = known_nodesArray;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}
