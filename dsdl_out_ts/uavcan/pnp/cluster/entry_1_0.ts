// Generated by llvmdsdl 0.2.3 (TypeScript backend).
// Source: uavcan.pnp.cluster.Entry.1.0
import * as dsdlRuntime from "../../../dsdl_runtime";

import { deserializeID, serializeID } from "../../node/id_1_0";
import type { ID } from "../../node/id_1_0";
export const LLVMDSDL_GENERATOR_VERSION = "0.2.3";
export const DSDL_FULL_NAME = "uavcan.pnp.cluster.Entry";
export const DSDL_IS_DEPRECATED = false;
export const DSDL_VERSION_MAJOR = 1;
export const DSDL_VERSION_MINOR = 0;
export const DSDL_REQUEST_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_REQUEST_ZOH_ALIAS_REASON = "invalid-bit-length";
export const DSDL_RESPONSE_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_RESPONSE_ZOH_ALIAS_REASON = "not-applicable";

// One PnP node-ID allocation entry.
// This type is a part of the Raft consensus algorithm. Please refer to the type AppendEntries for details.
export interface Entry {
  // Refer to the Raft paper for explanation.
  term: number;
  // Unique-ID of this allocation; zero if unknown.
  unique_id: Array<number>;
  // Node-ID of this allocation.
  node_id: ID;
}


export function serializeEntry(value: Entry): Uint8Array {
  const out = new Uint8Array(22);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_pnp_cluster_Entry_1_0 = (capacityBits: number): boolean => 176 <= capacityBits;
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_pnp_cluster_Entry_1_0__0__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 4294967295n ? 4294967295n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_pnp_cluster_Entry_1_0__1__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 255n ? 255n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_pnp_cluster_Entry_1_0(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 32, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_pnp_cluster_Entry_1_0__0__ser(value.term), true);
  offsetBits += 32;
  const unique_idArray = value.unique_id;
  if (!Array.isArray(unique_idArray) || unique_idArray.length !== 16) {
    throw new Error("field 'unique_id' expects exactly 16 elements");
  }
  for (let i = 0; i < 16; ++i) {
    dsdlRuntime.writeUnsigned(out, offsetBits, 8, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_pnp_cluster_Entry_1_0__1__ser(unique_idArray[i]), true);
    offsetBits += 8;
  }
  const node_idAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let node_idAlignBit = offsetBits; node_idAlignBit < node_idAlignedOffsetBits; ++node_idAlignBit) {
    dsdlRuntime.setBit(out, node_idAlignBit, false);
  }
  offsetBits = node_idAlignedOffsetBits;
  const node_idBytes = serializeID(value.node_id);
  dsdlRuntime.copyBits(out, offsetBits, node_idBytes, 0, 16);
  offsetBits += 16;
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeEntry(bytes: Uint8Array): { value: Entry; consumed: number } {
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_pnp_cluster_Entry_1_0__0__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 4294967295n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_pnp_cluster_Entry_1_0__1__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };

  const value = {} as Entry;
  let offsetBits = 0;
  const termRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 32);
  value.term = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_pnp_cluster_Entry_1_0__0__deser(termRaw));
  offsetBits += 32;
  const unique_idArray: Array<number> = new Array(16);
  for (let i = 0; i < 16; ++i) {
    const unique_idItemRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 8);
    unique_idArray[i] = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_pnp_cluster_Entry_1_0__1__deser(unique_idItemRaw));
    offsetBits += 8;
  }
  value.unique_id = unique_idArray;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const node_idBytes = dsdlRuntime.extractBits(bytes, offsetBits, 16);
  value.node_id = deserializeID(node_idBytes).value;
  offsetBits += 16;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}
