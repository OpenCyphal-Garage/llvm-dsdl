// Generated by llvmdsdl 0.2.3 (TypeScript backend).
// Source: uavcan.node.GetTransportStatistics.0.1
import * as dsdlRuntime from "../../dsdl_runtime";

import { deserializeIOStatistics, serializeIOStatistics } from "./io_statistics_0_1";
import type { IOStatistics } from "./io_statistics_0_1";
export const LLVMDSDL_GENERATOR_VERSION = "0.2.3";
export const DSDL_FULL_NAME = "uavcan.node.GetTransportStatistics";
export const DSDL_IS_DEPRECATED = false;
export const DSDL_VERSION_MAJOR = 0;
export const DSDL_VERSION_MINOR = 1;
export const DSDL_REQUEST_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_REQUEST_ZOH_ALIAS_REASON = "empty-layout";
export const DSDL_RESPONSE_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_RESPONSE_ZOH_ALIAS_REASON = "invalid-bit-length";

// Returns a set of general low-level transport statistical counters.
// Servers are encouraged but not required to sample the data atomically.
export interface GetTransportStatistics_Request {
}


export function serializeGetTransportStatistics_Request(value: GetTransportStatistics_Request): Uint8Array {
  const out = new Uint8Array(0);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_node_GetTransportStatistics_0_1__request = (capacityBits: number): boolean => 0 <= capacityBits;

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_node_GetTransportStatistics_0_1__request(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeGetTransportStatistics_Request(bytes: Uint8Array): { value: GetTransportStatistics_Request; consumed: number } {
  const value = {} as GetTransportStatistics_Request;
  let offsetBits = 0;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}

// Returns a set of general low-level transport statistical counters.
// Servers are encouraged but not required to sample the data atomically.
export interface GetTransportStatistics_Response {
  // Cyphal transfer performance statistics:
  // the number of Cyphal transfers successfully sent, successfully received, and failed.
  // The methods of error counting are implementation-defined.
  transfer_statistics: IOStatistics;
  // Network interface statistics, separate per interface.
  // E.g., for a doubly redundant transport, this array would contain two elements,
  // the one at the index zero would apply to the first interface, the other to the second interface.
  // The methods of counting are implementation-defined.
  network_interface_statistics: Array<IOStatistics>;
}

// Cyphal supports up to triply modular redundant interfaces.
export const GET_TRANSPORT_STATISTICS_RESPONSE_MAX_NETWORK_INTERFACES = 3;

export function serializeGetTransportStatistics_Response(value: GetTransportStatistics_Response): Uint8Array {
  const out = new Uint8Array(61);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_node_GetTransportStatistics_0_1__response = (capacityBits: number): boolean => 488 <= capacityBits;
  const mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_GetTransportStatistics_0_1__response__3__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_validate_array_length__uavcan_node_GetTransportStatistics_0_1__response__3 = (value: number): boolean => (value >= 0) && (value <= 3);

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_node_GetTransportStatistics_0_1__response(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  const transfer_statisticsAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let transfer_statisticsAlignBit = offsetBits; transfer_statisticsAlignBit < transfer_statisticsAlignedOffsetBits; ++transfer_statisticsAlignBit) {
    dsdlRuntime.setBit(out, transfer_statisticsAlignBit, false);
  }
  offsetBits = transfer_statisticsAlignedOffsetBits;
  const transfer_statisticsBytes = serializeIOStatistics(value.transfer_statistics);
  dsdlRuntime.copyBits(out, offsetBits, transfer_statisticsBytes, 0, 120);
  offsetBits += 120;
  const network_interface_statisticsAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let network_interface_statisticsAlignBit = offsetBits; network_interface_statisticsAlignBit < network_interface_statisticsAlignedOffsetBits; ++network_interface_statisticsAlignBit) {
    dsdlRuntime.setBit(out, network_interface_statisticsAlignBit, false);
  }
  offsetBits = network_interface_statisticsAlignedOffsetBits;
  const network_interface_statisticsArray = value.network_interface_statistics;
  if (!Array.isArray(network_interface_statisticsArray)) {
    throw new Error("field 'network_interface_statistics' expects an array");
  }
  if (!mlir_llvmdsdl_plan_validate_array_length__uavcan_node_GetTransportStatistics_0_1__response__3(network_interface_statisticsArray.length)) {
    throw new Error("field 'network_interface_statistics' exceeds max length 3");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 8, Number(mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_GetTransportStatistics_0_1__response__3__ser(network_interface_statisticsArray.length)), false);
  offsetBits += 8;
  for (let i = 0; i < network_interface_statisticsArray.length; ++i) {
    const network_interface_statisticsBytes = serializeIOStatistics(network_interface_statisticsArray[i]);
    dsdlRuntime.copyBits(out, offsetBits, network_interface_statisticsBytes, 0, 120);
    offsetBits += 120;
  }
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeGetTransportStatistics_Response(bytes: Uint8Array): { value: GetTransportStatistics_Response; consumed: number } {
  const mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_GetTransportStatistics_0_1__response__3__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_validate_array_length__uavcan_node_GetTransportStatistics_0_1__response__3 = (value: number): boolean => (value >= 0) && (value <= 3);

  const value = {} as GetTransportStatistics_Response;
  let offsetBits = 0;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const transfer_statisticsBytes = dsdlRuntime.extractBits(bytes, offsetBits, 120);
  value.transfer_statistics = deserializeIOStatistics(transfer_statisticsBytes).value;
  offsetBits += 120;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const network_interface_statisticsLengthRaw = Math.trunc(dsdlRuntime.readUnsigned(bytes, offsetBits, 8));
  offsetBits += 8;
  const network_interface_statisticsLength = Number(mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_GetTransportStatistics_0_1__response__3__deser(network_interface_statisticsLengthRaw));
  if (!mlir_llvmdsdl_plan_validate_array_length__uavcan_node_GetTransportStatistics_0_1__response__3(network_interface_statisticsLength)) {
    throw new Error("decoded length for field 'network_interface_statistics' exceeds max length 3");
  }
  const network_interface_statisticsArray: Array<IOStatistics> = new Array(network_interface_statisticsLength);
  for (let i = 0; i < network_interface_statisticsLength; ++i) {
    const network_interface_statisticsBytes = dsdlRuntime.extractBits(bytes, offsetBits, 120);
    network_interface_statisticsArray[i] = deserializeIOStatistics(network_interface_statisticsBytes).value;
    offsetBits += 120;
  }
  value.network_interface_statistics = network_interface_statisticsArray;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}

export type GetTransportStatistics = GetTransportStatistics_Request;
