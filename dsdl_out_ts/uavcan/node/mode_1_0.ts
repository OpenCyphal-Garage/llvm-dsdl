// Generated by llvmdsdl 0.2.3 (TypeScript backend).
// Source: uavcan.node.Mode.1.0
import * as dsdlRuntime from "../../dsdl_runtime";

export const LLVMDSDL_GENERATOR_VERSION = "0.2.3";
export const DSDL_FULL_NAME = "uavcan.node.Mode";
export const DSDL_IS_DEPRECATED = false;
export const DSDL_VERSION_MAJOR = 1;
export const DSDL_VERSION_MINOR = 0;
export const DSDL_REQUEST_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_REQUEST_ZOH_ALIAS_REASON = "sub-byte-field";
export const DSDL_RESPONSE_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_RESPONSE_ZOH_ALIAS_REASON = "not-applicable";

// The operating mode of a node.
// Reserved values can be used in future revisions of the specification.
export interface Mode {
  value: number;
}

// Normal operating mode.
export const MODE_OPERATIONAL = 0;
// Initialization is in progress; this mode is entered immediately after startup.
export const MODE_INITIALIZATION = 1;
// E.g., calibration, self-test, etc.
export const MODE_MAINTENANCE = 2;
// New software/firmware is being loaded or the bootloader is running.
export const MODE_SOFTWARE_UPDATE = 3;

export function serializeMode(value: Mode): Uint8Array {
  const out = new Uint8Array(1);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_node_Mode_1_0 = (capacityBits: number): boolean => 8 <= capacityBits;
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_Mode_1_0__0__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 7n ? 7n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_node_Mode_1_0(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 3, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_Mode_1_0__0__ser(value.value), true);
  offsetBits += 3;
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeMode(bytes: Uint8Array): { value: Mode; consumed: number } {
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_Mode_1_0__0__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 7n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };

  const value = {} as Mode;
  let offsetBits = 0;
  const valueRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 3);
  value.value = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_Mode_1_0__0__deser(valueRaw));
  offsetBits += 3;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}
