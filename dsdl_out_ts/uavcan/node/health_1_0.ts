// Generated by llvmdsdl 0.2.3 (TypeScript backend).
// Source: uavcan.node.Health.1.0
import * as dsdlRuntime from "../../dsdl_runtime";

export const LLVMDSDL_GENERATOR_VERSION = "0.2.3";
export const DSDL_FULL_NAME = "uavcan.node.Health";
export const DSDL_IS_DEPRECATED = false;
export const DSDL_VERSION_MAJOR = 1;
export const DSDL_VERSION_MINOR = 0;
export const DSDL_REQUEST_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_REQUEST_ZOH_ALIAS_REASON = "sub-byte-field";
export const DSDL_RESPONSE_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_RESPONSE_ZOH_ALIAS_REASON = "not-applicable";

// Abstract component health information. If the node performs multiple activities (provides multiple network services),
// its health status should reflect the status of the worst-performing activity (network service).
// Follows:
//   https://www.law.cornell.edu/cfr/text/14/23.1322
//   https://www.faa.gov/documentLibrary/media/Advisory_Circular/AC_25.1322-1.pdf section 6
export interface Health {
  value: number;
}

// The component is functioning properly (nominal).
export const HEALTH_NOMINAL = 0;
// A critical parameter went out of range or the component encountered a minor failure that does not prevent
// the subsystem from performing any of its real-time functions.
export const HEALTH_ADVISORY = 1;
// The component encountered a major failure and is performing in a degraded mode or outside of its designed limitations.
export const HEALTH_CAUTION = 2;
// The component suffered a fatal malfunction and is unable to perform its intended function.
export const HEALTH_WARNING = 3;

export function serializeHealth(value: Health): Uint8Array {
  const out = new Uint8Array(1);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_node_Health_1_0 = (capacityBits: number): boolean => 8 <= capacityBits;
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_Health_1_0__0__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 3n ? 3n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_node_Health_1_0(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 2, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_Health_1_0__0__ser(value.value), true);
  offsetBits += 2;
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeHealth(bytes: Uint8Array): { value: Health; consumed: number } {
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_Health_1_0__0__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 3n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };

  const value = {} as Health;
  let offsetBits = 0;
  const valueRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 2);
  value.value = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_Health_1_0__0__deser(valueRaw));
  offsetBits += 2;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}
