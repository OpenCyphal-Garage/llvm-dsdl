// Generated by llvmdsdl 0.2.3 (TypeScript backend).
// Source: uavcan.node.ExecuteCommand.1.3
import * as dsdlRuntime from "../../dsdl_runtime";

export const LLVMDSDL_GENERATOR_VERSION = "0.2.3";
export const DSDL_FULL_NAME = "uavcan.node.ExecuteCommand";
export const DSDL_IS_DEPRECATED = false;
export const DSDL_VERSION_MAJOR = 1;
export const DSDL_VERSION_MINOR = 3;
export const DSDL_REQUEST_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_REQUEST_ZOH_ALIAS_REASON = "variable-array";
export const DSDL_RESPONSE_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_RESPONSE_ZOH_ALIAS_REASON = "variable-array";

// Instructs the server node to execute or commence execution of a simple predefined command.
// All standard commands are optional; i.e., not guaranteed to be supported by all nodes.
export interface ExecuteCommand_Request {
  // Standard pre-defined commands are at the top of the range (defined below).
  // Vendors can define arbitrary, vendor-specific commands in the bottom part of the range (starting from zero).
  // Vendor-specific commands shall not use identifiers above 32767.
  command: number;
  // A string parameter supplied to the command. The format and interpretation is command-specific.
  // The standard commands do not use this field (ignore it), excepting the following:
  //   - COMMAND_BEGIN_SOFTWARE_UPDATE
  parameter: Array<number>;
}

// Reboot the node.
// Note that some standard commands may or may not require a restart in order to take effect; e.g., factory reset.
export const EXECUTE_COMMAND_REQUEST_COMMAND_RESTART = 65535;
// Shut down the node; further access will not be possible until the power is turned back on.
export const EXECUTE_COMMAND_REQUEST_COMMAND_POWER_OFF = 65534;
// Begin the software update process using uavcan.file.Read. This command makes use of the "parameter" field below.
// The parameter contains the path to the new software image file to be downloaded by the server from the client
// using the standard service uavcan.file.Read. Observe that this operation swaps the roles of the client and
// the server.
//
// Upon reception of this command, the server (updatee) will evaluate whether it is possible to begin the
// software update process. If that is deemed impossible, the command will be rejected with one of the
// error codes defined in the response section of this definition (e.g., BAD_STATE if the node is currently
// on-duty and a sudden interruption of its activities is considered unsafe, and so on).
// If an update process is already underway, the updatee should abort the process and restart with the new file,
// unless the updatee can determine that the specified file is the same file that is already being downloaded,
// in which case it is allowed to respond SUCCESS and continue the old update process.
// If there are no other conditions precluding the requested update, the updatee will return a SUCCESS and
// initiate the file transfer process by invoking the standard service uavcan.file.Read repeatedly until the file
// is transferred fully (please refer to the documentation for that data type for more information about its usage).
//
// While the software is being updated, the updatee should set its mode (the field "mode" in uavcan.node.Heartbeat)
// to MODE_SOFTWARE_UPDATE. Please refer to the documentation for uavcan.node.Heartbeat for more information.
//
// It is recognized that most systems will have to interrupt their normal services to perform the software update
// (unless some form of software hot swapping is implemented, as is the case in some high-availability systems).
//
// Microcontrollers that are requested to update their firmware may need to stop execution of their current firmware
// and start the embedded bootloader (although other approaches are possible as well). In that case,
// while the embedded bootloader is running, the mode reported via the message uavcan.node.Heartbeat should be
// MODE_SOFTWARE_UPDATE as long as the bootloader is runing, even if no update-related activities
// are currently underway. For example, if the update process failed and the bootloader cannot load the software,
// the same mode MODE_SOFTWARE_UPDATE will be reported.
// It is also recognized that in a microcontroller setting, the application that served the update request will have
// to pass the update-related metadata (such as the node-ID of the server and the firmware image file path) to
// the embedded bootloader. The tactics of that transaction lie outside of the scope of this specification.
export const EXECUTE_COMMAND_REQUEST_COMMAND_BEGIN_SOFTWARE_UPDATE = 65533;
// Return the node's configuration back to the factory default settings (may require restart).
// Due to the uncertainty whether a restart is required, generic interfaces should always force a restart.
export const EXECUTE_COMMAND_REQUEST_COMMAND_FACTORY_RESET = 65532;
// Cease activities immediately, enter a safe state until restarted.
// Further operation may no longer be possible until a restart command is executed.
export const EXECUTE_COMMAND_REQUEST_COMMAND_EMERGENCY_STOP = 65531;
// This command instructs the node to store the current configuration parameter values and other persistent states
// to the non-volatile storage. Nodes are allowed to manage persistent states automatically, obviating the need for
// this command by committing all such data to the non-volatile memory automatically as necessary. However, some
// nodes may lack this functionality, in which case this parameter should be used. Generic interfaces should always
// invoke this command in order to ensure that the data is stored even if the node doesn't implement automatic
// persistence management.
export const EXECUTE_COMMAND_REQUEST_COMMAND_STORE_PERSISTENT_STATES = 65530;
// This command instructs the node to physically identify itself in some way--e.g., by flashing a light or
// emitting a sound. The duration and the nature of the identification process is implementation-defined.
// This command can be useful for human operators to match assigned node-ID values to physical nodes during setup.
export const EXECUTE_COMMAND_REQUEST_COMMAND_IDENTIFY = 65529;

export function serializeExecuteCommand_Request(value: ExecuteCommand_Request): Uint8Array {
  const out = new Uint8Array(258);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_node_ExecuteCommand_1_3__request = (capacityBits: number): boolean => 2064 <= capacityBits;
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_ExecuteCommand_1_3__request__0__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 65535n ? 65535n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_ExecuteCommand_1_3__request__1__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 255n ? 255n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };
  const mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_ExecuteCommand_1_3__request__1__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_validate_array_length__uavcan_node_ExecuteCommand_1_3__request__1 = (value: number): boolean => (value >= 0) && (value <= 255);

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_node_ExecuteCommand_1_3__request(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 16, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_ExecuteCommand_1_3__request__0__ser(value.command), true);
  offsetBits += 16;
  const parameterArray = value.parameter;
  if (!Array.isArray(parameterArray)) {
    throw new Error("field 'parameter' expects an array");
  }
  if (!mlir_llvmdsdl_plan_validate_array_length__uavcan_node_ExecuteCommand_1_3__request__1(parameterArray.length)) {
    throw new Error("field 'parameter' exceeds max length 255");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 8, Number(mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_ExecuteCommand_1_3__request__1__ser(parameterArray.length)), false);
  offsetBits += 8;
  for (let i = 0; i < parameterArray.length; ++i) {
    dsdlRuntime.writeUnsigned(out, offsetBits, 8, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_ExecuteCommand_1_3__request__1__ser(parameterArray[i]), true);
    offsetBits += 8;
  }
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeExecuteCommand_Request(bytes: Uint8Array): { value: ExecuteCommand_Request; consumed: number } {
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_ExecuteCommand_1_3__request__0__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 65535n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_ExecuteCommand_1_3__request__1__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_ExecuteCommand_1_3__request__1__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_validate_array_length__uavcan_node_ExecuteCommand_1_3__request__1 = (value: number): boolean => (value >= 0) && (value <= 255);

  const value = {} as ExecuteCommand_Request;
  let offsetBits = 0;
  const commandRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 16);
  value.command = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_ExecuteCommand_1_3__request__0__deser(commandRaw));
  offsetBits += 16;
  const parameterLengthRaw = Math.trunc(dsdlRuntime.readUnsigned(bytes, offsetBits, 8));
  offsetBits += 8;
  const parameterLength = Number(mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_ExecuteCommand_1_3__request__1__deser(parameterLengthRaw));
  if (!mlir_llvmdsdl_plan_validate_array_length__uavcan_node_ExecuteCommand_1_3__request__1(parameterLength)) {
    throw new Error("decoded length for field 'parameter' exceeds max length 255");
  }
  const parameterArray: Array<number> = new Array(parameterLength);
  for (let i = 0; i < parameterLength; ++i) {
    const parameterItemRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 8);
    parameterArray[i] = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_ExecuteCommand_1_3__request__1__deser(parameterItemRaw));
    offsetBits += 8;
  }
  value.parameter = parameterArray;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}

// Instructs the server node to execute or commence execution of a simple predefined command.
// All standard commands are optional; i.e., not guaranteed to be supported by all nodes.
export interface ExecuteCommand_Response {
  // The result of the request.
  status: number;
  // Any output that could be useful that has the capability to convey detailed information.
  // Users can send commands and receive specific data, like device status or measurements back in a streamlined manner.
  // The standard commands should leave this field empty unless explicitly specified otherwise.
  output: Array<number>;
}

// Started or executed successfully
export const EXECUTE_COMMAND_RESPONSE_STATUS_SUCCESS = 0;
// Could not start or the desired outcome could not be reached
export const EXECUTE_COMMAND_RESPONSE_STATUS_FAILURE = 1;
// Denied due to lack of authorization
export const EXECUTE_COMMAND_RESPONSE_STATUS_NOT_AUTHORIZED = 2;
// The requested command is not known or not supported
export const EXECUTE_COMMAND_RESPONSE_STATUS_BAD_COMMAND = 3;
// The supplied parameter cannot be used with the selected command
export const EXECUTE_COMMAND_RESPONSE_STATUS_BAD_PARAMETER = 4;
// The current state of the node does not permit execution of this command
export const EXECUTE_COMMAND_RESPONSE_STATUS_BAD_STATE = 5;
// The operation should have succeeded but an unexpected failure occurred
export const EXECUTE_COMMAND_RESPONSE_STATUS_INTERNAL_ERROR = 6;

export function serializeExecuteCommand_Response(value: ExecuteCommand_Response): Uint8Array {
  const out = new Uint8Array(48);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_node_ExecuteCommand_1_3__response = (capacityBits: number): boolean => 384 <= capacityBits;
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_ExecuteCommand_1_3__response__0__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 255n ? 255n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_ExecuteCommand_1_3__response__1__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 255n ? 255n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };
  const mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_ExecuteCommand_1_3__response__1__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_validate_array_length__uavcan_node_ExecuteCommand_1_3__response__1 = (value: number): boolean => (value >= 0) && (value <= 46);

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_node_ExecuteCommand_1_3__response(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 8, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_ExecuteCommand_1_3__response__0__ser(value.status), true);
  offsetBits += 8;
  const outputArray = value.output;
  if (!Array.isArray(outputArray)) {
    throw new Error("field 'output' expects an array");
  }
  if (!mlir_llvmdsdl_plan_validate_array_length__uavcan_node_ExecuteCommand_1_3__response__1(outputArray.length)) {
    throw new Error("field 'output' exceeds max length 46");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 8, Number(mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_ExecuteCommand_1_3__response__1__ser(outputArray.length)), false);
  offsetBits += 8;
  for (let i = 0; i < outputArray.length; ++i) {
    dsdlRuntime.writeUnsigned(out, offsetBits, 8, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_ExecuteCommand_1_3__response__1__ser(outputArray[i]), true);
    offsetBits += 8;
  }
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeExecuteCommand_Response(bytes: Uint8Array): { value: ExecuteCommand_Response; consumed: number } {
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_ExecuteCommand_1_3__response__0__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_ExecuteCommand_1_3__response__1__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_ExecuteCommand_1_3__response__1__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_validate_array_length__uavcan_node_ExecuteCommand_1_3__response__1 = (value: number): boolean => (value >= 0) && (value <= 46);

  const value = {} as ExecuteCommand_Response;
  let offsetBits = 0;
  const statusRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 8);
  value.status = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_ExecuteCommand_1_3__response__0__deser(statusRaw));
  offsetBits += 8;
  const outputLengthRaw = Math.trunc(dsdlRuntime.readUnsigned(bytes, offsetBits, 8));
  offsetBits += 8;
  const outputLength = Number(mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_ExecuteCommand_1_3__response__1__deser(outputLengthRaw));
  if (!mlir_llvmdsdl_plan_validate_array_length__uavcan_node_ExecuteCommand_1_3__response__1(outputLength)) {
    throw new Error("decoded length for field 'output' exceeds max length 46");
  }
  const outputArray: Array<number> = new Array(outputLength);
  for (let i = 0; i < outputLength; ++i) {
    const outputItemRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 8);
    outputArray[i] = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_ExecuteCommand_1_3__response__1__deser(outputItemRaw));
    offsetBits += 8;
  }
  value.output = outputArray;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}

export type ExecuteCommand = ExecuteCommand_Request;
