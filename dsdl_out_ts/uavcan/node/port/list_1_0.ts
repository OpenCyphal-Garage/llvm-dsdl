// Generated by llvmdsdl 0.2.3 (TypeScript backend).
// Source: uavcan.node.port.List.1.0
import * as dsdlRuntime from "../../../dsdl_runtime";

import { deserializeServiceIDList, serializeServiceIDList } from "./service_id_list_1_0";
import { deserializeSubjectIDList, serializeSubjectIDList } from "./subject_id_list_1_0";
import type { ServiceIDList } from "./service_id_list_1_0";
import type { SubjectIDList } from "./subject_id_list_1_0";
export const LLVMDSDL_GENERATOR_VERSION = "0.2.3";
export const DSDL_FULL_NAME = "uavcan.node.port.List";
export const DSDL_IS_DEPRECATED = false;
export const DSDL_VERSION_MAJOR = 1;
export const DSDL_VERSION_MINOR = 0;
export const DSDL_REQUEST_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_REQUEST_ZOH_ALIAS_REASON = "invalid-bit-length";
export const DSDL_RESPONSE_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_RESPONSE_ZOH_ALIAS_REASON = "not-applicable";

// A list of ports that this node is using:
// - Subjects published by this node (whether periodically or ad-hoc).
// - Subjects that this node is subscribed to (a datalogger or a debugger would typically subscribe to all subjects).
// - RPC services consumed by this node (i.e., service clients).
// - RPC services provided by this node (i.e., service servers).
//
// All nodes should implement this capability to provide network introspection and diagnostic capabilities.
// This message should be published using the fixed subject-ID as follows:
// - At the OPTIONAL priority level at least every MAX_PUBLICATION_PERIOD seconds.
// - At the OPTIONAL or SLOW priority level within MAX_PUBLICATION_PERIOD after the port configuration is changed.
export interface List {
  publishers: SubjectIDList;
  subscribers: SubjectIDList;
  clients: ServiceIDList;
  servers: ServiceIDList;
}

// [seconds]
// If the port configuration is not updated in this amount of time, the node should publish this message anyway.
export const LIST_MAX_PUBLICATION_PERIOD = 10;

export function serializeList(value: List): Uint8Array {
  const out = new Uint8Array(8466);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_node_port_List_1_0 = (capacityBits: number): boolean => 67728 <= capacityBits;
  const mlir_llvmdsdl_plan_validate_delimiter_header__uavcan_node_port_List_1_0__1 = (payloadBytes: number, remainingBytes: number): boolean => (payloadBytes >= 0) && (payloadBytes <= remainingBytes);
  const mlir_llvmdsdl_plan_validate_delimiter_header__uavcan_node_port_List_1_0__3 = (payloadBytes: number, remainingBytes: number): boolean => (payloadBytes >= 0) && (payloadBytes <= remainingBytes);
  const mlir_llvmdsdl_plan_validate_delimiter_header__uavcan_node_port_List_1_0__5 = (payloadBytes: number, remainingBytes: number): boolean => (payloadBytes >= 0) && (payloadBytes <= remainingBytes);
  const mlir_llvmdsdl_plan_validate_delimiter_header__uavcan_node_port_List_1_0__7 = (payloadBytes: number, remainingBytes: number): boolean => (payloadBytes >= 0) && (payloadBytes <= remainingBytes);

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_node_port_List_1_0(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  const publishersAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let publishersAlignBit = offsetBits; publishersAlignBit < publishersAlignedOffsetBits; ++publishersAlignBit) {
    dsdlRuntime.setBit(out, publishersAlignBit, false);
  }
  offsetBits = publishersAlignedOffsetBits;
  const publishersBytes = serializeSubjectIDList(value.publishers);
  const publishersSizeBytes = publishersBytes.length;
  if (publishersSizeBytes > 4101) {
    throw new Error("encoded payload for composite field 'publishers' exceeds max payload bytes 4101");
  }
  const publishersRemainingBytes = out.length - Math.min(Math.trunc(offsetBits / 8), out.length);
  if (publishersSizeBytes > publishersRemainingBytes) {
    throw new Error("encoded payload for composite field 'publishers' exceeds remaining buffer space");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 32, publishersSizeBytes, false);
  offsetBits += 32;
  dsdlRuntime.copyBits(out, offsetBits, publishersBytes, 0, publishersSizeBytes * 8);
  offsetBits += publishersSizeBytes * 8;
  const subscribersAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let subscribersAlignBit = offsetBits; subscribersAlignBit < subscribersAlignedOffsetBits; ++subscribersAlignBit) {
    dsdlRuntime.setBit(out, subscribersAlignBit, false);
  }
  offsetBits = subscribersAlignedOffsetBits;
  const subscribersBytes = serializeSubjectIDList(value.subscribers);
  const subscribersSizeBytes = subscribersBytes.length;
  if (subscribersSizeBytes > 4101) {
    throw new Error("encoded payload for composite field 'subscribers' exceeds max payload bytes 4101");
  }
  const subscribersRemainingBytes = out.length - Math.min(Math.trunc(offsetBits / 8), out.length);
  if (subscribersSizeBytes > subscribersRemainingBytes) {
    throw new Error("encoded payload for composite field 'subscribers' exceeds remaining buffer space");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 32, subscribersSizeBytes, false);
  offsetBits += 32;
  dsdlRuntime.copyBits(out, offsetBits, subscribersBytes, 0, subscribersSizeBytes * 8);
  offsetBits += subscribersSizeBytes * 8;
  const clientsAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let clientsAlignBit = offsetBits; clientsAlignBit < clientsAlignedOffsetBits; ++clientsAlignBit) {
    dsdlRuntime.setBit(out, clientsAlignBit, false);
  }
  offsetBits = clientsAlignedOffsetBits;
  const clientsBytes = serializeServiceIDList(value.clients);
  const clientsSizeBytes = clientsBytes.length;
  if (clientsSizeBytes > 132) {
    throw new Error("encoded payload for composite field 'clients' exceeds max payload bytes 132");
  }
  const clientsRemainingBytes = out.length - Math.min(Math.trunc(offsetBits / 8), out.length);
  if (clientsSizeBytes > clientsRemainingBytes) {
    throw new Error("encoded payload for composite field 'clients' exceeds remaining buffer space");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 32, clientsSizeBytes, false);
  offsetBits += 32;
  dsdlRuntime.copyBits(out, offsetBits, clientsBytes, 0, clientsSizeBytes * 8);
  offsetBits += clientsSizeBytes * 8;
  const serversAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let serversAlignBit = offsetBits; serversAlignBit < serversAlignedOffsetBits; ++serversAlignBit) {
    dsdlRuntime.setBit(out, serversAlignBit, false);
  }
  offsetBits = serversAlignedOffsetBits;
  const serversBytes = serializeServiceIDList(value.servers);
  const serversSizeBytes = serversBytes.length;
  if (serversSizeBytes > 132) {
    throw new Error("encoded payload for composite field 'servers' exceeds max payload bytes 132");
  }
  const serversRemainingBytes = out.length - Math.min(Math.trunc(offsetBits / 8), out.length);
  if (serversSizeBytes > serversRemainingBytes) {
    throw new Error("encoded payload for composite field 'servers' exceeds remaining buffer space");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 32, serversSizeBytes, false);
  offsetBits += 32;
  dsdlRuntime.copyBits(out, offsetBits, serversBytes, 0, serversSizeBytes * 8);
  offsetBits += serversSizeBytes * 8;
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeList(bytes: Uint8Array): { value: List; consumed: number } {
  const mlir_llvmdsdl_plan_validate_delimiter_header__uavcan_node_port_List_1_0__1 = (payloadBytes: number, remainingBytes: number): boolean => (payloadBytes >= 0) && (payloadBytes <= remainingBytes);
  const mlir_llvmdsdl_plan_validate_delimiter_header__uavcan_node_port_List_1_0__3 = (payloadBytes: number, remainingBytes: number): boolean => (payloadBytes >= 0) && (payloadBytes <= remainingBytes);
  const mlir_llvmdsdl_plan_validate_delimiter_header__uavcan_node_port_List_1_0__5 = (payloadBytes: number, remainingBytes: number): boolean => (payloadBytes >= 0) && (payloadBytes <= remainingBytes);
  const mlir_llvmdsdl_plan_validate_delimiter_header__uavcan_node_port_List_1_0__7 = (payloadBytes: number, remainingBytes: number): boolean => (payloadBytes >= 0) && (payloadBytes <= remainingBytes);

  const value = {} as List;
  let offsetBits = 0;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const publishersSizeBytes = Math.trunc(dsdlRuntime.readUnsigned(bytes, offsetBits, 32));
  offsetBits += 32;
  const publishersRemainingBytes = bytes.length - Math.min(Math.trunc(offsetBits / 8), bytes.length);
  if (!mlir_llvmdsdl_plan_validate_delimiter_header__uavcan_node_port_List_1_0__1(publishersSizeBytes, publishersRemainingBytes)) {
    throw new Error("decoded payload size for composite field 'publishers' exceeds remaining buffer space");
  }
  const publishersStartByte = Math.min(Math.trunc(offsetBits / 8), bytes.length);
  const publishersEndByte = Math.min(publishersStartByte + publishersSizeBytes, bytes.length);
  const publishersBytes = bytes.subarray(publishersStartByte, publishersEndByte);
  value.publishers = deserializeSubjectIDList(publishersBytes).value;
  offsetBits += publishersSizeBytes * 8;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const subscribersSizeBytes = Math.trunc(dsdlRuntime.readUnsigned(bytes, offsetBits, 32));
  offsetBits += 32;
  const subscribersRemainingBytes = bytes.length - Math.min(Math.trunc(offsetBits / 8), bytes.length);
  if (!mlir_llvmdsdl_plan_validate_delimiter_header__uavcan_node_port_List_1_0__3(subscribersSizeBytes, subscribersRemainingBytes)) {
    throw new Error("decoded payload size for composite field 'subscribers' exceeds remaining buffer space");
  }
  const subscribersStartByte = Math.min(Math.trunc(offsetBits / 8), bytes.length);
  const subscribersEndByte = Math.min(subscribersStartByte + subscribersSizeBytes, bytes.length);
  const subscribersBytes = bytes.subarray(subscribersStartByte, subscribersEndByte);
  value.subscribers = deserializeSubjectIDList(subscribersBytes).value;
  offsetBits += subscribersSizeBytes * 8;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const clientsSizeBytes = Math.trunc(dsdlRuntime.readUnsigned(bytes, offsetBits, 32));
  offsetBits += 32;
  const clientsRemainingBytes = bytes.length - Math.min(Math.trunc(offsetBits / 8), bytes.length);
  if (!mlir_llvmdsdl_plan_validate_delimiter_header__uavcan_node_port_List_1_0__5(clientsSizeBytes, clientsRemainingBytes)) {
    throw new Error("decoded payload size for composite field 'clients' exceeds remaining buffer space");
  }
  const clientsStartByte = Math.min(Math.trunc(offsetBits / 8), bytes.length);
  const clientsEndByte = Math.min(clientsStartByte + clientsSizeBytes, bytes.length);
  const clientsBytes = bytes.subarray(clientsStartByte, clientsEndByte);
  value.clients = deserializeServiceIDList(clientsBytes).value;
  offsetBits += clientsSizeBytes * 8;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const serversSizeBytes = Math.trunc(dsdlRuntime.readUnsigned(bytes, offsetBits, 32));
  offsetBits += 32;
  const serversRemainingBytes = bytes.length - Math.min(Math.trunc(offsetBits / 8), bytes.length);
  if (!mlir_llvmdsdl_plan_validate_delimiter_header__uavcan_node_port_List_1_0__7(serversSizeBytes, serversRemainingBytes)) {
    throw new Error("decoded payload size for composite field 'servers' exceeds remaining buffer space");
  }
  const serversStartByte = Math.min(Math.trunc(offsetBits / 8), bytes.length);
  const serversEndByte = Math.min(serversStartByte + serversSizeBytes, bytes.length);
  const serversBytes = bytes.subarray(serversStartByte, serversEndByte);
  value.servers = deserializeServiceIDList(serversBytes).value;
  offsetBits += serversSizeBytes * 8;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}
