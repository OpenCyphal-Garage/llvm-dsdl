// Generated by llvmdsdl 0.2.3 (TypeScript backend).
// Source: uavcan.node.port.SubjectIDList.1.0
import * as dsdlRuntime from "../../../dsdl_runtime";

import { deserializeEmpty, serializeEmpty } from "../../primitive/empty_1_0";
import { deserializeSubjectID, serializeSubjectID } from "./subject_id_1_0";
import type { Empty } from "../../primitive/empty_1_0";
import type { SubjectID } from "./subject_id_1_0";
export const LLVMDSDL_GENERATOR_VERSION = "0.2.3";
export const DSDL_FULL_NAME = "uavcan.node.port.SubjectIDList";
export const DSDL_IS_DEPRECATED = false;
export const DSDL_VERSION_MAJOR = 1;
export const DSDL_VERSION_MINOR = 0;
export const DSDL_REQUEST_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_REQUEST_ZOH_ALIAS_REASON = "sub-byte-field";
export const DSDL_RESPONSE_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_RESPONSE_ZOH_ALIAS_REASON = "not-applicable";

// A list of subject identifiers.
// The range of subject-ID is large, so using a fixed-size bitmask would make this type difficult to handle on
// resource-constrained systems. To address that, we provide two extra options: a simple variable-length list,
// and a special case that indicates that every subject-ID is in use.
export type SubjectIDList =
// The index represents the identifier value. True -- present/used. False -- absent/unused.
  | { _tag: 0; mask: Array<boolean>; }
// A list of identifiers that can be used instead of the mask if most of the identifiers are unused.
  | { _tag: 1; sparse_list: Array<SubjectID>; }
// A special case indicating that all identifiers are in use.
  | { _tag: 2; total: Empty; };

export const SUBJECT_ID_LIST_CAPACITY = 8192;

export function serializeSubjectIDList(value: SubjectIDList): Uint8Array {
  const out = new Uint8Array(1025);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_node_port_SubjectIDList_1_0 = (capacityBits: number): boolean => 8200 <= capacityBits;
  const mlir_llvmdsdl_plan_validate_union_tag__uavcan_node_port_SubjectIDList_1_0 = (tagValue: number): boolean => (tagValue === 0) || (tagValue === 1) || (tagValue === 2);
  const mlir_llvmdsdl_plan_union_tag__uavcan_node_port_SubjectIDList_1_0__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_port_SubjectIDList_1_0__2__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_validate_array_length__uavcan_node_port_SubjectIDList_1_0__2 = (value: number): boolean => (value >= 0) && (value <= 255);

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_node_port_SubjectIDList_1_0(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  let tag = Math.trunc((value as { _tag: number })._tag);
  if (!mlir_llvmdsdl_plan_validate_union_tag__uavcan_node_port_SubjectIDList_1_0(tag)) {
    throw new Error("invalid union tag " + tag);
  }
  tag = Number(mlir_llvmdsdl_plan_union_tag__uavcan_node_port_SubjectIDList_1_0__ser(tag));
  dsdlRuntime.writeUnsigned(out, offsetBits, 8, tag, false);
  offsetBits += 8;
  switch (tag) {
  case 0: {
    const optionValue = (value as Record<string, unknown>).mask;
    if (optionValue === undefined) {
      throw new Error("union field 'mask' missing for tag 0");
    }
    const maskArray = optionValue;
    if (!Array.isArray(maskArray) || maskArray.length !== 8192) {
      throw new Error("union field 'mask' expects exactly 8192 elements");
    }
    for (let i = 0; i < 8192; ++i) {
      dsdlRuntime.setBit(out, offsetBits, maskArray[i]);
      offsetBits += 1;
    }
    break;
  }
  case 1: {
    const optionValue = (value as Record<string, unknown>).sparse_list;
    if (optionValue === undefined) {
      throw new Error("union field 'sparse_list' missing for tag 1");
    }
    const sparse_listOptionAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
    for (let sparse_listOptionAlignBit = offsetBits; sparse_listOptionAlignBit < sparse_listOptionAlignedOffsetBits; ++sparse_listOptionAlignBit) {
      dsdlRuntime.setBit(out, sparse_listOptionAlignBit, false);
    }
    offsetBits = sparse_listOptionAlignedOffsetBits;
    const sparse_listArray = optionValue;
    if (!Array.isArray(sparse_listArray)) {
      throw new Error("union field 'sparse_list' expects an array");
    }
    if (!mlir_llvmdsdl_plan_validate_array_length__uavcan_node_port_SubjectIDList_1_0__2(sparse_listArray.length)) {
      throw new Error("union field 'sparse_list' exceeds max length 255");
    }
    dsdlRuntime.writeUnsigned(out, offsetBits, 8, Number(mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_port_SubjectIDList_1_0__2__ser(sparse_listArray.length)), false);
    offsetBits += 8;
    for (let i = 0; i < sparse_listArray.length; ++i) {
      const sparse_listBytes = serializeSubjectID(sparse_listArray[i] as SubjectID);
      dsdlRuntime.copyBits(out, offsetBits, sparse_listBytes, 0, 16);
      offsetBits += 16;
    }
    break;
  }
  case 2: {
    const optionValue = (value as Record<string, unknown>).total;
    if (optionValue === undefined) {
      throw new Error("union field 'total' missing for tag 2");
    }
    const totalOptionAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
    for (let totalOptionAlignBit = offsetBits; totalOptionAlignBit < totalOptionAlignedOffsetBits; ++totalOptionAlignBit) {
      dsdlRuntime.setBit(out, totalOptionAlignBit, false);
    }
    offsetBits = totalOptionAlignedOffsetBits;
    const totalBytes = serializeEmpty(optionValue as Empty);
    dsdlRuntime.copyBits(out, offsetBits, totalBytes, 0, 0);
    offsetBits += 0;
    break;
  }
  default:
    throw new Error("invalid union tag " + tag);
  }
  const alignedOffsetBits = dsdlRuntime.byteLengthForBits(offsetBits) * 8;
  for (let bit = offsetBits; bit < alignedOffsetBits; ++bit) {
    dsdlRuntime.setBit(out, bit, false);
  }
  offsetBits = alignedOffsetBits;
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeSubjectIDList(bytes: Uint8Array): { value: SubjectIDList; consumed: number } {
  const mlir_llvmdsdl_plan_validate_union_tag__uavcan_node_port_SubjectIDList_1_0 = (tagValue: number): boolean => (tagValue === 0) || (tagValue === 1) || (tagValue === 2);
  const mlir_llvmdsdl_plan_union_tag__uavcan_node_port_SubjectIDList_1_0__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_port_SubjectIDList_1_0__2__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_validate_array_length__uavcan_node_port_SubjectIDList_1_0__2 = (value: number): boolean => (value >= 0) && (value <= 255);

  let offsetBits = 0;
  let tag = Math.trunc(dsdlRuntime.readUnsigned(bytes, offsetBits, 8));
  tag = Number(mlir_llvmdsdl_plan_union_tag__uavcan_node_port_SubjectIDList_1_0__deser(tag));
  if (!mlir_llvmdsdl_plan_validate_union_tag__uavcan_node_port_SubjectIDList_1_0(tag)) {
    throw new Error("decoded invalid union tag " + tag);
  }
  offsetBits += 8;
  let value: SubjectIDList;
  switch (tag) {
  case 0: {
    const maskArray: Array<boolean> = new Array(8192);
    for (let i = 0; i < 8192; ++i) {
      maskArray[i] = dsdlRuntime.getBit(bytes, offsetBits);
      offsetBits += 1;
    }
    const optionValue = maskArray;
    value = { _tag: 0, mask: optionValue } as SubjectIDList;
    break;
  }
  case 1: {
    offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
    const sparse_listLengthRaw = Math.trunc(dsdlRuntime.readUnsigned(bytes, offsetBits, 8));
    offsetBits += 8;
    const sparse_listLength = Number(mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_port_SubjectIDList_1_0__2__deser(sparse_listLengthRaw));
    if (!mlir_llvmdsdl_plan_validate_array_length__uavcan_node_port_SubjectIDList_1_0__2(sparse_listLength)) {
      throw new Error("decoded length for union field 'sparse_list' exceeds max length 255");
    }
    const sparse_listArray: Array<SubjectID> = new Array(sparse_listLength);
    for (let i = 0; i < sparse_listLength; ++i) {
      const sparse_listBytes = dsdlRuntime.extractBits(bytes, offsetBits, 16);
      sparse_listArray[i] = deserializeSubjectID(sparse_listBytes).value;
      offsetBits += 16;
    }
    const optionValue = sparse_listArray;
    value = { _tag: 1, sparse_list: optionValue } as SubjectIDList;
    break;
  }
  case 2: {
    offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
    let optionValue: Empty;
    const totalBytes = dsdlRuntime.extractBits(bytes, offsetBits, 0);
    optionValue = deserializeEmpty(totalBytes).value;
    offsetBits += 0;
    value = { _tag: 2, total: optionValue } as SubjectIDList;
    break;
  }
  default:
    throw new Error("decoded invalid union tag " + tag);
  }
  offsetBits = dsdlRuntime.byteLengthForBits(offsetBits) * 8;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}
