// Generated by llvmdsdl 0.2.3 (TypeScript backend).
// Source: uavcan.node.port.ServiceIDList.1.0
import * as dsdlRuntime from "../../../dsdl_runtime";

export const LLVMDSDL_GENERATOR_VERSION = "0.2.3";
export const DSDL_FULL_NAME = "uavcan.node.port.ServiceIDList";
export const DSDL_IS_DEPRECATED = false;
export const DSDL_VERSION_MAJOR = 1;
export const DSDL_VERSION_MINOR = 0;
export const DSDL_REQUEST_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_REQUEST_ZOH_ALIAS_REASON = "sub-byte-field";
export const DSDL_RESPONSE_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_RESPONSE_ZOH_ALIAS_REASON = "not-applicable";

// A list of service identifiers.
// This is a trivial constant-size bitmask with some reserved space in case the range of service-ID is increased
// in a future revision of the protocol.
export interface ServiceIDList {
  // The index represents the identifier value. True -- present/used. False -- absent/unused.
  mask: Array<boolean>;
}

export const SERVICE_ID_LIST_CAPACITY = 512;

export function serializeServiceIDList(value: ServiceIDList): Uint8Array {
  const out = new Uint8Array(64);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_node_port_ServiceIDList_1_0 = (capacityBits: number): boolean => 512 <= capacityBits;

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_node_port_ServiceIDList_1_0(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  const maskArray = value.mask;
  if (!Array.isArray(maskArray) || maskArray.length !== 512) {
    throw new Error("field 'mask' expects exactly 512 elements");
  }
  for (let i = 0; i < 512; ++i) {
    dsdlRuntime.setBit(out, offsetBits, maskArray[i]);
    offsetBits += 1;
  }
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeServiceIDList(bytes: Uint8Array): { value: ServiceIDList; consumed: number } {
  const value = {} as ServiceIDList;
  let offsetBits = 0;
  const maskArray: Array<boolean> = new Array(512);
  for (let i = 0; i < 512; ++i) {
    maskArray[i] = dsdlRuntime.getBit(bytes, offsetBits);
    offsetBits += 1;
  }
  value.mask = maskArray;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}
