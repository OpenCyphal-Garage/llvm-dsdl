// Generated by llvmdsdl 0.2.3 (TypeScript backend).
// Source: uavcan.node.GetInfo.1.0
import * as dsdlRuntime from "../../dsdl_runtime";

import { deserializeVersion, serializeVersion } from "./version_1_0";
import type { Version } from "./version_1_0";
export const LLVMDSDL_GENERATOR_VERSION = "0.2.3";
export const DSDL_FULL_NAME = "uavcan.node.GetInfo";
export const DSDL_IS_DEPRECATED = false;
export const DSDL_VERSION_MAJOR = 1;
export const DSDL_VERSION_MINOR = 0;
export const DSDL_REQUEST_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_REQUEST_ZOH_ALIAS_REASON = "empty-layout";
export const DSDL_RESPONSE_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_RESPONSE_ZOH_ALIAS_REASON = "invalid-bit-length";

// Full node info request.
// All of the returned information shall be static (unchanged) while the node is running.
// It is highly recommended to support this service on all nodes.
export interface GetInfo_Request {
}


export function serializeGetInfo_Request(value: GetInfo_Request): Uint8Array {
  const out = new Uint8Array(0);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_node_GetInfo_1_0__request = (capacityBits: number): boolean => 0 <= capacityBits;

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_node_GetInfo_1_0__request(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeGetInfo_Request(bytes: Uint8Array): { value: GetInfo_Request; consumed: number } {
  const value = {} as GetInfo_Request;
  let offsetBits = 0;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}

// Full node info request.
// All of the returned information shall be static (unchanged) while the node is running.
// It is highly recommended to support this service on all nodes.
export interface GetInfo_Response {
  // The Cyphal protocol version implemented on this node, both major and minor.
  // Not to be changed while the node is running.
  protocol_version: Version;
  hardware_version: Version;
  // The version information shall not be changed while the node is running.
  // The correct hardware version shall be reported at all times, excepting software-only nodes, in which
  // case it should be set to zeros.
  // If the node is equipped with a Cyphal-capable bootloader, the bootloader should report the software
  // version of the installed application, if there is any; if no application is found, zeros should be reported.
  software_version: Version;
  // A version control system (VCS) revision number or hash. Not to be changed while the node is running.
  // For example, this field can be used for reporting the short git commit hash of the current
  // software revision.
  // Set to zero if not used.
  software_vcs_revision_id: bigint;
  // The unique-ID (UID) is a 128-bit long sequence that is likely to be globally unique per node.
  // The vendor shall ensure that the probability of a collision with any other node UID globally is negligibly low.
  // UID is defined once per hardware unit and should never be changed.
  // All zeros is not a valid UID.
  // If the node is equipped with a Cyphal-capable bootloader, the bootloader shall use the same UID.
  unique_id: Array<number>;
  // Human-readable non-empty ASCII node name. An empty name is not permitted.
  // The name shall not be changed while the node is running.
  // Allowed characters are: a-z (lowercase ASCII letters) 0-9 (decimal digits) . (dot) - (dash) _ (underscore).
  // Node name is a reversed Internet domain name (like Java packages), e.g. "com.manufacturer.project.product".
  name: Array<number>;
  // The value of an arbitrary hash function applied to the software image. Not to be changed while the node is running.
  // This field can be used to detect whether the software or firmware running on the node is an exact
  // same version as a certain specific revision. This field provides a very strong identity guarantee,
  // unlike the version fields above, which can be the same for different builds of the software.
  // As can be seen from its definition, this field is optional.
  //
  // The exact hash function and the methods of its application are implementation-defined.
  // However, implementations are recommended to adhere to the following guidelines, fully or partially:
  //   - The hash function should be CRC-64-WE.
  //   - The hash function should be applied to the entire application image padded to 8 bytes.
  //   - If the computed image CRC is stored within the software image itself, the value of
  //     the hash function becomes ill-defined, because it becomes recursively dependent on itself.
  //     In order to circumvent this issue, while computing or checking the CRC, its value stored
  //     within the image should be zeroed out.
  software_image_crc: Array<bigint>;
  // The certificate of authenticity (COA) of the node, 222 bytes max, optional. This field can be used for
  // reporting digital signatures (e.g., RSA-1776, or ECDSA if a higher degree of cryptographic strength is desired).
  // Leave empty if not used. Not to be changed while the node is running.
  certificate_of_authenticity: Array<number>;
}


export function serializeGetInfo_Response(value: GetInfo_Response): Uint8Array {
  const out = new Uint8Array(313);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_node_GetInfo_1_0__response = (capacityBits: number): boolean => 2504 <= capacityBits;
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_GetInfo_1_0__response__6__ser = (value: number | bigint): number | bigint => value;
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_GetInfo_1_0__response__7__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 255n ? 255n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_GetInfo_1_0__response__8__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 255n ? 255n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_GetInfo_1_0__response__9__ser = (value: number | bigint): number | bigint => value;
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_GetInfo_1_0__response__10__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 255n ? 255n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };
  const mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_GetInfo_1_0__response__8__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_GetInfo_1_0__response__9__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_GetInfo_1_0__response__10__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_validate_array_length__uavcan_node_GetInfo_1_0__response__8 = (value: number): boolean => (value >= 0) && (value <= 50);
  const mlir_llvmdsdl_plan_validate_array_length__uavcan_node_GetInfo_1_0__response__9 = (value: number): boolean => (value >= 0) && (value <= 1);
  const mlir_llvmdsdl_plan_validate_array_length__uavcan_node_GetInfo_1_0__response__10 = (value: number): boolean => (value >= 0) && (value <= 222);

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_node_GetInfo_1_0__response(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  const protocol_versionAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let protocol_versionAlignBit = offsetBits; protocol_versionAlignBit < protocol_versionAlignedOffsetBits; ++protocol_versionAlignBit) {
    dsdlRuntime.setBit(out, protocol_versionAlignBit, false);
  }
  offsetBits = protocol_versionAlignedOffsetBits;
  const protocol_versionBytes = serializeVersion(value.protocol_version);
  dsdlRuntime.copyBits(out, offsetBits, protocol_versionBytes, 0, 16);
  offsetBits += 16;
  const hardware_versionAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let hardware_versionAlignBit = offsetBits; hardware_versionAlignBit < hardware_versionAlignedOffsetBits; ++hardware_versionAlignBit) {
    dsdlRuntime.setBit(out, hardware_versionAlignBit, false);
  }
  offsetBits = hardware_versionAlignedOffsetBits;
  const hardware_versionBytes = serializeVersion(value.hardware_version);
  dsdlRuntime.copyBits(out, offsetBits, hardware_versionBytes, 0, 16);
  offsetBits += 16;
  const software_versionAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let software_versionAlignBit = offsetBits; software_versionAlignBit < software_versionAlignedOffsetBits; ++software_versionAlignBit) {
    dsdlRuntime.setBit(out, software_versionAlignBit, false);
  }
  offsetBits = software_versionAlignedOffsetBits;
  const software_versionBytes = serializeVersion(value.software_version);
  dsdlRuntime.copyBits(out, offsetBits, software_versionBytes, 0, 16);
  offsetBits += 16;
  dsdlRuntime.writeUnsigned(out, offsetBits, 64, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_GetInfo_1_0__response__6__ser(value.software_vcs_revision_id), true);
  offsetBits += 64;
  const unique_idArray = value.unique_id;
  if (!Array.isArray(unique_idArray) || unique_idArray.length !== 16) {
    throw new Error("field 'unique_id' expects exactly 16 elements");
  }
  for (let i = 0; i < 16; ++i) {
    dsdlRuntime.writeUnsigned(out, offsetBits, 8, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_GetInfo_1_0__response__7__ser(unique_idArray[i]), true);
    offsetBits += 8;
  }
  const nameArray = value.name;
  if (!Array.isArray(nameArray)) {
    throw new Error("field 'name' expects an array");
  }
  if (!mlir_llvmdsdl_plan_validate_array_length__uavcan_node_GetInfo_1_0__response__8(nameArray.length)) {
    throw new Error("field 'name' exceeds max length 50");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 8, Number(mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_GetInfo_1_0__response__8__ser(nameArray.length)), false);
  offsetBits += 8;
  for (let i = 0; i < nameArray.length; ++i) {
    dsdlRuntime.writeUnsigned(out, offsetBits, 8, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_GetInfo_1_0__response__8__ser(nameArray[i]), true);
    offsetBits += 8;
  }
  const software_image_crcArray = value.software_image_crc;
  if (!Array.isArray(software_image_crcArray)) {
    throw new Error("field 'software_image_crc' expects an array");
  }
  if (!mlir_llvmdsdl_plan_validate_array_length__uavcan_node_GetInfo_1_0__response__9(software_image_crcArray.length)) {
    throw new Error("field 'software_image_crc' exceeds max length 1");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 8, Number(mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_GetInfo_1_0__response__9__ser(software_image_crcArray.length)), false);
  offsetBits += 8;
  for (let i = 0; i < software_image_crcArray.length; ++i) {
    dsdlRuntime.writeUnsigned(out, offsetBits, 64, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_GetInfo_1_0__response__9__ser(software_image_crcArray[i]), true);
    offsetBits += 64;
  }
  const certificate_of_authenticityArray = value.certificate_of_authenticity;
  if (!Array.isArray(certificate_of_authenticityArray)) {
    throw new Error("field 'certificate_of_authenticity' expects an array");
  }
  if (!mlir_llvmdsdl_plan_validate_array_length__uavcan_node_GetInfo_1_0__response__10(certificate_of_authenticityArray.length)) {
    throw new Error("field 'certificate_of_authenticity' exceeds max length 222");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 8, Number(mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_GetInfo_1_0__response__10__ser(certificate_of_authenticityArray.length)), false);
  offsetBits += 8;
  for (let i = 0; i < certificate_of_authenticityArray.length; ++i) {
    dsdlRuntime.writeUnsigned(out, offsetBits, 8, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_GetInfo_1_0__response__10__ser(certificate_of_authenticityArray[i]), true);
    offsetBits += 8;
  }
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeGetInfo_Response(bytes: Uint8Array): { value: GetInfo_Response; consumed: number } {
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_GetInfo_1_0__response__6__deser = (value: number | bigint): number | bigint => value;
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_GetInfo_1_0__response__7__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_GetInfo_1_0__response__8__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_GetInfo_1_0__response__9__deser = (value: number | bigint): number | bigint => value;
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_GetInfo_1_0__response__10__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_GetInfo_1_0__response__8__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_GetInfo_1_0__response__9__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_GetInfo_1_0__response__10__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_validate_array_length__uavcan_node_GetInfo_1_0__response__8 = (value: number): boolean => (value >= 0) && (value <= 50);
  const mlir_llvmdsdl_plan_validate_array_length__uavcan_node_GetInfo_1_0__response__9 = (value: number): boolean => (value >= 0) && (value <= 1);
  const mlir_llvmdsdl_plan_validate_array_length__uavcan_node_GetInfo_1_0__response__10 = (value: number): boolean => (value >= 0) && (value <= 222);

  const value = {} as GetInfo_Response;
  let offsetBits = 0;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const protocol_versionBytes = dsdlRuntime.extractBits(bytes, offsetBits, 16);
  value.protocol_version = deserializeVersion(protocol_versionBytes).value;
  offsetBits += 16;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const hardware_versionBytes = dsdlRuntime.extractBits(bytes, offsetBits, 16);
  value.hardware_version = deserializeVersion(hardware_versionBytes).value;
  offsetBits += 16;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const software_versionBytes = dsdlRuntime.extractBits(bytes, offsetBits, 16);
  value.software_version = deserializeVersion(software_versionBytes).value;
  offsetBits += 16;
  const software_vcs_revision_idRaw = dsdlRuntime.readUnsignedBigInt(bytes, offsetBits, 64);
  value.software_vcs_revision_id = BigInt(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_GetInfo_1_0__response__6__deser(software_vcs_revision_idRaw));
  offsetBits += 64;
  const unique_idArray: Array<number> = new Array(16);
  for (let i = 0; i < 16; ++i) {
    const unique_idItemRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 8);
    unique_idArray[i] = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_GetInfo_1_0__response__7__deser(unique_idItemRaw));
    offsetBits += 8;
  }
  value.unique_id = unique_idArray;
  const nameLengthRaw = Math.trunc(dsdlRuntime.readUnsigned(bytes, offsetBits, 8));
  offsetBits += 8;
  const nameLength = Number(mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_GetInfo_1_0__response__8__deser(nameLengthRaw));
  if (!mlir_llvmdsdl_plan_validate_array_length__uavcan_node_GetInfo_1_0__response__8(nameLength)) {
    throw new Error("decoded length for field 'name' exceeds max length 50");
  }
  const nameArray: Array<number> = new Array(nameLength);
  for (let i = 0; i < nameLength; ++i) {
    const nameItemRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 8);
    nameArray[i] = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_GetInfo_1_0__response__8__deser(nameItemRaw));
    offsetBits += 8;
  }
  value.name = nameArray;
  const software_image_crcLengthRaw = Math.trunc(dsdlRuntime.readUnsigned(bytes, offsetBits, 8));
  offsetBits += 8;
  const software_image_crcLength = Number(mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_GetInfo_1_0__response__9__deser(software_image_crcLengthRaw));
  if (!mlir_llvmdsdl_plan_validate_array_length__uavcan_node_GetInfo_1_0__response__9(software_image_crcLength)) {
    throw new Error("decoded length for field 'software_image_crc' exceeds max length 1");
  }
  const software_image_crcArray: Array<bigint> = new Array(software_image_crcLength);
  for (let i = 0; i < software_image_crcLength; ++i) {
    const software_image_crcItemRaw = dsdlRuntime.readUnsignedBigInt(bytes, offsetBits, 64);
    software_image_crcArray[i] = BigInt(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_GetInfo_1_0__response__9__deser(software_image_crcItemRaw));
    offsetBits += 64;
  }
  value.software_image_crc = software_image_crcArray;
  const certificate_of_authenticityLengthRaw = Math.trunc(dsdlRuntime.readUnsigned(bytes, offsetBits, 8));
  offsetBits += 8;
  const certificate_of_authenticityLength = Number(mlir_llvmdsdl_plan_array_length_prefix__uavcan_node_GetInfo_1_0__response__10__deser(certificate_of_authenticityLengthRaw));
  if (!mlir_llvmdsdl_plan_validate_array_length__uavcan_node_GetInfo_1_0__response__10(certificate_of_authenticityLength)) {
    throw new Error("decoded length for field 'certificate_of_authenticity' exceeds max length 222");
  }
  const certificate_of_authenticityArray: Array<number> = new Array(certificate_of_authenticityLength);
  for (let i = 0; i < certificate_of_authenticityLength; ++i) {
    const certificate_of_authenticityItemRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 8);
    certificate_of_authenticityArray[i] = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_GetInfo_1_0__response__10__deser(certificate_of_authenticityItemRaw));
    offsetBits += 8;
  }
  value.certificate_of_authenticity = certificate_of_authenticityArray;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}

export type GetInfo = GetInfo_Request;
