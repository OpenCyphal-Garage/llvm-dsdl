// Generated by llvmdsdl 0.2.3 (TypeScript backend).
// Source: uavcan.node.Heartbeat.1.0
import * as dsdlRuntime from "../../dsdl_runtime";

import { deserializeHealth, serializeHealth } from "./health_1_0";
import { deserializeMode, serializeMode } from "./mode_1_0";
import type { Health } from "./health_1_0";
import type { Mode } from "./mode_1_0";
export const LLVMDSDL_GENERATOR_VERSION = "0.2.3";
export const DSDL_FULL_NAME = "uavcan.node.Heartbeat";
export const DSDL_IS_DEPRECATED = false;
export const DSDL_VERSION_MAJOR = 1;
export const DSDL_VERSION_MINOR = 0;
export const DSDL_REQUEST_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_REQUEST_ZOH_ALIAS_REASON = "invalid-bit-length";
export const DSDL_RESPONSE_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_RESPONSE_ZOH_ALIAS_REASON = "not-applicable";

// Abstract node status information.
// This is the only high-level function that shall be implemented by all nodes.
//
// All Cyphal nodes that have a node-ID are required to publish this message to its fixed subject periodically.
// Nodes that do not have a node-ID (also known as "anonymous nodes") shall not publish to this subject.
//
// The default subject-ID 7509 is 1110101010101 in binary. The alternating bit pattern at the end helps transceiver
// synchronization (e.g., on CAN-based networks) and on some transports permits automatic bit rate detection.
//
// Network-wide health monitoring can be implemented by subscribing to the fixed subject.
export interface Heartbeat {
  // [second]
  // The uptime seconds counter should never overflow. The counter will reach the upper limit in ~136 years,
  // upon which time it should stay at 0xFFFFFFFF until the node is restarted.
  // Other nodes may detect that a remote node has restarted when this value leaps backwards.
  uptime: number;
  // The abstract health status of this node.
  health: Health;
  // The abstract operating mode of the publishing node.
  // This field indicates the general level of readiness that can be further elaborated on a per-activity basis
  // using various specialized interfaces.
  mode: Mode;
  // Optional, vendor-specific node status code, e.g. a fault code or a status bitmask.
  vendor_specific_status_code: number;
}

// [second]
// The publication period shall not exceed this limit.
// The period should not change while the node is running.
export const HEARTBEAT_MAX_PUBLICATION_PERIOD = 1;
// [second]
// If the last message from the node was received more than this amount of time ago, it should be considered offline.
export const HEARTBEAT_OFFLINE_TIMEOUT = 3;

export function serializeHeartbeat(value: Heartbeat): Uint8Array {
  const out = new Uint8Array(7);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_node_Heartbeat_1_0 = (capacityBits: number): boolean => 56 <= capacityBits;
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_Heartbeat_1_0__0__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 4294967295n ? 4294967295n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_Heartbeat_1_0__5__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 255n ? 255n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_node_Heartbeat_1_0(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 32, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_Heartbeat_1_0__0__ser(value.uptime), true);
  offsetBits += 32;
  const healthAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let healthAlignBit = offsetBits; healthAlignBit < healthAlignedOffsetBits; ++healthAlignBit) {
    dsdlRuntime.setBit(out, healthAlignBit, false);
  }
  offsetBits = healthAlignedOffsetBits;
  const healthBytes = serializeHealth(value.health);
  dsdlRuntime.copyBits(out, offsetBits, healthBytes, 0, 8);
  offsetBits += 8;
  const modeAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let modeAlignBit = offsetBits; modeAlignBit < modeAlignedOffsetBits; ++modeAlignBit) {
    dsdlRuntime.setBit(out, modeAlignBit, false);
  }
  offsetBits = modeAlignedOffsetBits;
  const modeBytes = serializeMode(value.mode);
  dsdlRuntime.copyBits(out, offsetBits, modeBytes, 0, 8);
  offsetBits += 8;
  dsdlRuntime.writeUnsigned(out, offsetBits, 8, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_Heartbeat_1_0__5__ser(value.vendor_specific_status_code), true);
  offsetBits += 8;
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeHeartbeat(bytes: Uint8Array): { value: Heartbeat; consumed: number } {
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_Heartbeat_1_0__0__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 4294967295n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_Heartbeat_1_0__5__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };

  const value = {} as Heartbeat;
  let offsetBits = 0;
  const uptimeRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 32);
  value.uptime = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_Heartbeat_1_0__0__deser(uptimeRaw));
  offsetBits += 32;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const healthBytes = dsdlRuntime.extractBits(bytes, offsetBits, 8);
  value.health = deserializeHealth(healthBytes).value;
  offsetBits += 8;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const modeBytes = dsdlRuntime.extractBits(bytes, offsetBits, 8);
  value.mode = deserializeMode(modeBytes).value;
  offsetBits += 8;
  const vendor_specific_status_codeRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 8);
  value.vendor_specific_status_code = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_node_Heartbeat_1_0__5__deser(vendor_specific_status_codeRaw));
  offsetBits += 8;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}
