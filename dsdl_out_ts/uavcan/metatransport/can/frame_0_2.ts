// Generated by llvmdsdl 0.2.3 (TypeScript backend).
// Source: uavcan.metatransport.can.Frame.0.2
import * as dsdlRuntime from "../../../dsdl_runtime";

import { deserializeDataClassic, serializeDataClassic } from "./data_classic_0_1";
import { deserializeDataFD, serializeDataFD } from "./data_fd_0_1";
import { deserializeError, serializeError } from "./error_0_1";
import { deserializeRTR, serializeRTR } from "./rtr_0_1";
import type { DataClassic } from "./data_classic_0_1";
import type { DataFD } from "./data_fd_0_1";
import type { Error } from "./error_0_1";
import type { RTR } from "./rtr_0_1";
export const LLVMDSDL_GENERATOR_VERSION = "0.2.3";
export const DSDL_FULL_NAME = "uavcan.metatransport.can.Frame";
export const DSDL_IS_DEPRECATED = false;
export const DSDL_VERSION_MAJOR = 0;
export const DSDL_VERSION_MINOR = 2;
export const DSDL_REQUEST_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_REQUEST_ZOH_ALIAS_REASON = "invalid-bit-length";
export const DSDL_RESPONSE_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_RESPONSE_ZOH_ALIAS_REASON = "not-applicable";

// Classic CAN or CAN FD frame representation. This is the top-level data type in its namespace.
export type Frame =
// CAN error (intentional or disturbance)
  | { _tag: 0; error: Error; }
// Bit rate switch flag active
  | { _tag: 1; data_fd: DataFD; }
// Bit rate switch flag not active
  | { _tag: 2; data_classic: DataClassic; }
// Bit rate switch flag not active
  | { _tag: 3; remote_transmission_request: RTR; };


export function serializeFrame(value: Frame): Uint8Array {
  const out = new Uint8Array(71);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_metatransport_can_Frame_0_2 = (capacityBits: number): boolean => 568 <= capacityBits;
  const mlir_llvmdsdl_plan_validate_union_tag__uavcan_metatransport_can_Frame_0_2 = (tagValue: number): boolean => (tagValue === 0) || (tagValue === 1) || (tagValue === 2) || (tagValue === 3);
  const mlir_llvmdsdl_plan_union_tag__uavcan_metatransport_can_Frame_0_2__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_metatransport_can_Frame_0_2(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  let tag = Math.trunc((value as { _tag: number })._tag);
  if (!mlir_llvmdsdl_plan_validate_union_tag__uavcan_metatransport_can_Frame_0_2(tag)) {
    throw new Error("invalid union tag " + tag);
  }
  tag = Number(mlir_llvmdsdl_plan_union_tag__uavcan_metatransport_can_Frame_0_2__ser(tag));
  dsdlRuntime.writeUnsigned(out, offsetBits, 8, tag, false);
  offsetBits += 8;
  switch (tag) {
  case 0: {
    const optionValue = (value as Record<string, unknown>).error;
    if (optionValue === undefined) {
      throw new Error("union field 'error' missing for tag 0");
    }
    const errorOptionAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
    for (let errorOptionAlignBit = offsetBits; errorOptionAlignBit < errorOptionAlignedOffsetBits; ++errorOptionAlignBit) {
      dsdlRuntime.setBit(out, errorOptionAlignBit, false);
    }
    offsetBits = errorOptionAlignedOffsetBits;
    const errorBytes = serializeError(optionValue as Error);
    dsdlRuntime.copyBits(out, offsetBits, errorBytes, 0, 32);
    offsetBits += 32;
    break;
  }
  case 1: {
    const optionValue = (value as Record<string, unknown>).data_fd;
    if (optionValue === undefined) {
      throw new Error("union field 'data_fd' missing for tag 1");
    }
    const data_fdOptionAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
    for (let data_fdOptionAlignBit = offsetBits; data_fdOptionAlignBit < data_fdOptionAlignedOffsetBits; ++data_fdOptionAlignBit) {
      dsdlRuntime.setBit(out, data_fdOptionAlignBit, false);
    }
    offsetBits = data_fdOptionAlignedOffsetBits;
    const data_fdBytes = serializeDataFD(optionValue as DataFD);
    dsdlRuntime.copyBits(out, offsetBits, data_fdBytes, 0, 560);
    offsetBits += 560;
    break;
  }
  case 2: {
    const optionValue = (value as Record<string, unknown>).data_classic;
    if (optionValue === undefined) {
      throw new Error("union field 'data_classic' missing for tag 2");
    }
    const data_classicOptionAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
    for (let data_classicOptionAlignBit = offsetBits; data_classicOptionAlignBit < data_classicOptionAlignedOffsetBits; ++data_classicOptionAlignBit) {
      dsdlRuntime.setBit(out, data_classicOptionAlignBit, false);
    }
    offsetBits = data_classicOptionAlignedOffsetBits;
    const data_classicBytes = serializeDataClassic(optionValue as DataClassic);
    dsdlRuntime.copyBits(out, offsetBits, data_classicBytes, 0, 112);
    offsetBits += 112;
    break;
  }
  case 3: {
    const optionValue = (value as Record<string, unknown>).remote_transmission_request;
    if (optionValue === undefined) {
      throw new Error("union field 'remote_transmission_request' missing for tag 3");
    }
    const remote_transmission_requestOptionAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
    for (let remote_transmission_requestOptionAlignBit = offsetBits; remote_transmission_requestOptionAlignBit < remote_transmission_requestOptionAlignedOffsetBits; ++remote_transmission_requestOptionAlignBit) {
      dsdlRuntime.setBit(out, remote_transmission_requestOptionAlignBit, false);
    }
    offsetBits = remote_transmission_requestOptionAlignedOffsetBits;
    const remote_transmission_requestBytes = serializeRTR(optionValue as RTR);
    dsdlRuntime.copyBits(out, offsetBits, remote_transmission_requestBytes, 0, 40);
    offsetBits += 40;
    break;
  }
  default:
    throw new Error("invalid union tag " + tag);
  }
  const alignedOffsetBits = dsdlRuntime.byteLengthForBits(offsetBits) * 8;
  for (let bit = offsetBits; bit < alignedOffsetBits; ++bit) {
    dsdlRuntime.setBit(out, bit, false);
  }
  offsetBits = alignedOffsetBits;
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeFrame(bytes: Uint8Array): { value: Frame; consumed: number } {
  const mlir_llvmdsdl_plan_validate_union_tag__uavcan_metatransport_can_Frame_0_2 = (tagValue: number): boolean => (tagValue === 0) || (tagValue === 1) || (tagValue === 2) || (tagValue === 3);
  const mlir_llvmdsdl_plan_union_tag__uavcan_metatransport_can_Frame_0_2__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };

  let offsetBits = 0;
  let tag = Math.trunc(dsdlRuntime.readUnsigned(bytes, offsetBits, 8));
  tag = Number(mlir_llvmdsdl_plan_union_tag__uavcan_metatransport_can_Frame_0_2__deser(tag));
  if (!mlir_llvmdsdl_plan_validate_union_tag__uavcan_metatransport_can_Frame_0_2(tag)) {
    throw new Error("decoded invalid union tag " + tag);
  }
  offsetBits += 8;
  let value: Frame;
  switch (tag) {
  case 0: {
    offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
    let optionValue: Error;
    const errorBytes = dsdlRuntime.extractBits(bytes, offsetBits, 32);
    optionValue = deserializeError(errorBytes).value;
    offsetBits += 32;
    value = { _tag: 0, error: optionValue } as Frame;
    break;
  }
  case 1: {
    offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
    let optionValue: DataFD;
    const data_fdBytes = dsdlRuntime.extractBits(bytes, offsetBits, 560);
    optionValue = deserializeDataFD(data_fdBytes).value;
    offsetBits += 560;
    value = { _tag: 1, data_fd: optionValue } as Frame;
    break;
  }
  case 2: {
    offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
    let optionValue: DataClassic;
    const data_classicBytes = dsdlRuntime.extractBits(bytes, offsetBits, 112);
    optionValue = deserializeDataClassic(data_classicBytes).value;
    offsetBits += 112;
    value = { _tag: 2, data_classic: optionValue } as Frame;
    break;
  }
  case 3: {
    offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
    let optionValue: RTR;
    const remote_transmission_requestBytes = dsdlRuntime.extractBits(bytes, offsetBits, 40);
    optionValue = deserializeRTR(remote_transmission_requestBytes).value;
    offsetBits += 40;
    value = { _tag: 3, remote_transmission_request: optionValue } as Frame;
    break;
  }
  default:
    throw new Error("decoded invalid union tag " + tag);
  }
  offsetBits = dsdlRuntime.byteLengthForBits(offsetBits) * 8;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}
