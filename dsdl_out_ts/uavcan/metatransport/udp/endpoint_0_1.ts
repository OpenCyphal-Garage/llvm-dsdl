// Generated by llvmdsdl 0.2.3 (TypeScript backend).
// Source: uavcan.metatransport.udp.Endpoint.0.1
import * as dsdlRuntime from "../../../dsdl_runtime";

export const LLVMDSDL_GENERATOR_VERSION = "0.2.3";
export const DSDL_FULL_NAME = "uavcan.metatransport.udp.Endpoint";
export const DSDL_IS_DEPRECATED = true;
export const DSDL_VERSION_MAJOR = 0;
export const DSDL_VERSION_MINOR = 1;
export const DSDL_REQUEST_ZOH_ALIAS_ELIGIBLE = true;
export const DSDL_REQUEST_ZOH_ALIAS_REASON = "eligible";
export const DSDL_RESPONSE_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_RESPONSE_ZOH_ALIAS_REASON = "not-applicable";

// A UDP/IP endpoint address specification.
/**
 * @deprecated uavcan.metatransport.udp.Endpoint.0.1 is marked @deprecated in its
 *   DSDL definition and is scheduled for removal; migrate to a newer
 *   version of this type.
 */
export interface Endpoint {
  // The IP address of the host in the network byte order (big endian).
  // IPv6 addresses are represented as-is.
  // IPv4 addresses are represented using IPv4-mapped IPv6 addresses.
  ip_address: Array<number>;
  // MAC address of the host in the network byte order (big endian).
  mac_address: Array<number>;
  // The UDP port number.
  port: number;
}


export function serializeEndpoint(value: Endpoint): Uint8Array {
  const out = new Uint8Array(32);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_metatransport_udp_Endpoint_0_1 = (capacityBits: number): boolean => 256 <= capacityBits;
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_metatransport_udp_Endpoint_0_1__0__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 255n ? 255n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_metatransport_udp_Endpoint_0_1__1__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 255n ? 255n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_metatransport_udp_Endpoint_0_1__2__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 65535n ? 65535n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_metatransport_udp_Endpoint_0_1(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  const ip_addressArray = value.ip_address;
  if (!Array.isArray(ip_addressArray) || ip_addressArray.length !== 16) {
    throw new Error("field 'ip_address' expects exactly 16 elements");
  }
  for (let i = 0; i < 16; ++i) {
    dsdlRuntime.writeUnsigned(out, offsetBits, 8, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_metatransport_udp_Endpoint_0_1__0__ser(ip_addressArray[i]), true);
    offsetBits += 8;
  }
  const mac_addressArray = value.mac_address;
  if (!Array.isArray(mac_addressArray) || mac_addressArray.length !== 6) {
    throw new Error("field 'mac_address' expects exactly 6 elements");
  }
  for (let i = 0; i < 6; ++i) {
    dsdlRuntime.writeUnsigned(out, offsetBits, 8, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_metatransport_udp_Endpoint_0_1__1__ser(mac_addressArray[i]), true);
    offsetBits += 8;
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 16, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_metatransport_udp_Endpoint_0_1__2__ser(value.port), true);
  offsetBits += 16;
  for (let _PaddingBit = 0; _PaddingBit < 64; ++_PaddingBit) {
    dsdlRuntime.setBit(out, offsetBits + _PaddingBit, false);
  }
  offsetBits += 64;
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeEndpoint(bytes: Uint8Array): { value: Endpoint; consumed: number } {
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_metatransport_udp_Endpoint_0_1__0__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_metatransport_udp_Endpoint_0_1__1__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_metatransport_udp_Endpoint_0_1__2__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 65535n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };

  const value = {} as Endpoint;
  let offsetBits = 0;
  const ip_addressArray: Array<number> = new Array(16);
  for (let i = 0; i < 16; ++i) {
    const ip_addressItemRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 8);
    ip_addressArray[i] = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_metatransport_udp_Endpoint_0_1__0__deser(ip_addressItemRaw));
    offsetBits += 8;
  }
  value.ip_address = ip_addressArray;
  const mac_addressArray: Array<number> = new Array(6);
  for (let i = 0; i < 6; ++i) {
    const mac_addressItemRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 8);
    mac_addressArray[i] = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_metatransport_udp_Endpoint_0_1__1__deser(mac_addressItemRaw));
    offsetBits += 8;
  }
  value.mac_address = mac_addressArray;
  const portRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 16);
  value.port = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_metatransport_udp_Endpoint_0_1__2__deser(portRaw));
  offsetBits += 16;
  offsetBits += 64;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}
