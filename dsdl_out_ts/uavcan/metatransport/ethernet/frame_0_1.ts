// Generated by llvmdsdl 0.2.3 (TypeScript backend).
// Source: uavcan.metatransport.ethernet.Frame.0.1
import * as dsdlRuntime from "../../../dsdl_runtime";

import { deserializeEtherType, serializeEtherType } from "./ether_type_0_1";
import type { EtherType } from "./ether_type_0_1";
export const LLVMDSDL_GENERATOR_VERSION = "0.2.3";
export const DSDL_FULL_NAME = "uavcan.metatransport.ethernet.Frame";
export const DSDL_IS_DEPRECATED = false;
export const DSDL_VERSION_MAJOR = 0;
export const DSDL_VERSION_MINOR = 1;
export const DSDL_REQUEST_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_REQUEST_ZOH_ALIAS_REASON = "invalid-bit-length";
export const DSDL_RESPONSE_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_RESPONSE_ZOH_ALIAS_REASON = "not-applicable";

// IEEE 802.3 Ethernet frame encapsulation.
// In terms of libpcap/tcpdump, the corresponding link type is LINKTYPE_ETHERNET/DLT_EN10MB.
export interface Frame {
  destination: Array<number>;
  source: Array<number>;
  ethertype: EtherType;
  // Supports conventional jumbo frames (up to 9 KiB).
  payload: Array<number>;
}


export function serializeFrame(value: Frame): Uint8Array {
  const out = new Uint8Array(9232);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_metatransport_ethernet_Frame_0_1 = (capacityBits: number): boolean => 73856 <= capacityBits;
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_metatransport_ethernet_Frame_0_1__0__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 255n ? 255n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_metatransport_ethernet_Frame_0_1__1__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 255n ? 255n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_metatransport_ethernet_Frame_0_1__4__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 255n ? 255n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };
  const mlir_llvmdsdl_plan_array_length_prefix__uavcan_metatransport_ethernet_Frame_0_1__4__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 65535n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_validate_array_length__uavcan_metatransport_ethernet_Frame_0_1__4 = (value: number): boolean => (value >= 0) && (value <= 9216);

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_metatransport_ethernet_Frame_0_1(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  const destinationArray = value.destination;
  if (!Array.isArray(destinationArray) || destinationArray.length !== 6) {
    throw new Error("field 'destination' expects exactly 6 elements");
  }
  for (let i = 0; i < 6; ++i) {
    dsdlRuntime.writeUnsigned(out, offsetBits, 8, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_metatransport_ethernet_Frame_0_1__0__ser(destinationArray[i]), true);
    offsetBits += 8;
  }
  const sourceArray = value.source;
  if (!Array.isArray(sourceArray) || sourceArray.length !== 6) {
    throw new Error("field 'source' expects exactly 6 elements");
  }
  for (let i = 0; i < 6; ++i) {
    dsdlRuntime.writeUnsigned(out, offsetBits, 8, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_metatransport_ethernet_Frame_0_1__1__ser(sourceArray[i]), true);
    offsetBits += 8;
  }
  const ethertypeAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let ethertypeAlignBit = offsetBits; ethertypeAlignBit < ethertypeAlignedOffsetBits; ++ethertypeAlignBit) {
    dsdlRuntime.setBit(out, ethertypeAlignBit, false);
  }
  offsetBits = ethertypeAlignedOffsetBits;
  const ethertypeBytes = serializeEtherType(value.ethertype);
  dsdlRuntime.copyBits(out, offsetBits, ethertypeBytes, 0, 16);
  offsetBits += 16;
  const payloadArray = value.payload;
  if (!Array.isArray(payloadArray)) {
    throw new Error("field 'payload' expects an array");
  }
  if (!mlir_llvmdsdl_plan_validate_array_length__uavcan_metatransport_ethernet_Frame_0_1__4(payloadArray.length)) {
    throw new Error("field 'payload' exceeds max length 9216");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 16, Number(mlir_llvmdsdl_plan_array_length_prefix__uavcan_metatransport_ethernet_Frame_0_1__4__ser(payloadArray.length)), false);
  offsetBits += 16;
  for (let i = 0; i < payloadArray.length; ++i) {
    dsdlRuntime.writeUnsigned(out, offsetBits, 8, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_metatransport_ethernet_Frame_0_1__4__ser(payloadArray[i]), true);
    offsetBits += 8;
  }
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeFrame(bytes: Uint8Array): { value: Frame; consumed: number } {
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_metatransport_ethernet_Frame_0_1__0__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_metatransport_ethernet_Frame_0_1__1__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_metatransport_ethernet_Frame_0_1__4__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 255n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_array_length_prefix__uavcan_metatransport_ethernet_Frame_0_1__4__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 65535n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };
  const mlir_llvmdsdl_plan_validate_array_length__uavcan_metatransport_ethernet_Frame_0_1__4 = (value: number): boolean => (value >= 0) && (value <= 9216);

  const value = {} as Frame;
  let offsetBits = 0;
  const destinationArray: Array<number> = new Array(6);
  for (let i = 0; i < 6; ++i) {
    const destinationItemRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 8);
    destinationArray[i] = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_metatransport_ethernet_Frame_0_1__0__deser(destinationItemRaw));
    offsetBits += 8;
  }
  value.destination = destinationArray;
  const sourceArray: Array<number> = new Array(6);
  for (let i = 0; i < 6; ++i) {
    const sourceItemRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 8);
    sourceArray[i] = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_metatransport_ethernet_Frame_0_1__1__deser(sourceItemRaw));
    offsetBits += 8;
  }
  value.source = sourceArray;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const ethertypeBytes = dsdlRuntime.extractBits(bytes, offsetBits, 16);
  value.ethertype = deserializeEtherType(ethertypeBytes).value;
  offsetBits += 16;
  const payloadLengthRaw = Math.trunc(dsdlRuntime.readUnsigned(bytes, offsetBits, 16));
  offsetBits += 16;
  const payloadLength = Number(mlir_llvmdsdl_plan_array_length_prefix__uavcan_metatransport_ethernet_Frame_0_1__4__deser(payloadLengthRaw));
  if (!mlir_llvmdsdl_plan_validate_array_length__uavcan_metatransport_ethernet_Frame_0_1__4(payloadLength)) {
    throw new Error("decoded length for field 'payload' exceeds max length 9216");
  }
  const payloadArray: Array<number> = new Array(payloadLength);
  for (let i = 0; i < payloadLength; ++i) {
    const payloadItemRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 8);
    payloadArray[i] = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_metatransport_ethernet_Frame_0_1__4__deser(payloadItemRaw));
    offsetBits += 8;
  }
  value.payload = payloadArray;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}
