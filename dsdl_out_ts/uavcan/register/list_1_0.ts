// Generated by llvmdsdl 0.2.3 (TypeScript backend).
// Source: uavcan.register.List.1.0
import * as dsdlRuntime from "../../dsdl_runtime";

import { deserializeName, serializeName } from "./name_1_0";
import type { Name } from "./name_1_0";
export const LLVMDSDL_GENERATOR_VERSION = "0.2.3";
export const DSDL_FULL_NAME = "uavcan.register.List";
export const DSDL_IS_DEPRECATED = false;
export const DSDL_VERSION_MAJOR = 1;
export const DSDL_VERSION_MINOR = 0;
export const DSDL_REQUEST_ZOH_ALIAS_ELIGIBLE = true;
export const DSDL_REQUEST_ZOH_ALIAS_REASON = "eligible";
export const DSDL_RESPONSE_ZOH_ALIAS_ELIGIBLE = false;
export const DSDL_RESPONSE_ZOH_ALIAS_REASON = "invalid-bit-length";

// This service allows the caller to discover the names of all registers available on the server
// by iterating the index field from zero until an empty name is returned.
//
// The ordering of the registers shall remain constant while the server is running.
// The ordering is not guaranteed to remain unchanged when the server node is restarted.
export interface List_Request {
  index: number;
}


export function serializeList_Request(value: List_Request): Uint8Array {
  const out = new Uint8Array(2);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_register_List_1_0__request = (capacityBits: number): boolean => 16 <= capacityBits;
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_register_List_1_0__request__0__ser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const clamped = raw < 0n ? 0n : (raw > 65535n ? 65535n : raw);
    return (typeof value === "bigint") ? clamped : Number(clamped);
  };

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_register_List_1_0__request(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  dsdlRuntime.writeUnsigned(out, offsetBits, 16, mlir_llvmdsdl_plan_scalar_unsigned__uavcan_register_List_1_0__request__0__ser(value.index), true);
  offsetBits += 16;
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeList_Request(bytes: Uint8Array): { value: List_Request; consumed: number } {
  const mlir_llvmdsdl_plan_scalar_unsigned__uavcan_register_List_1_0__request__0__deser = (value: number | bigint): number | bigint => {
    const raw = (typeof value === "bigint") ? value : BigInt(Math.trunc(value));
    const masked = raw & 65535n;
    return (typeof value === "bigint") ? masked : Number(masked);
  };

  const value = {} as List_Request;
  let offsetBits = 0;
  const indexRaw = dsdlRuntime.readUnsigned(bytes, offsetBits, 16);
  value.index = Number(mlir_llvmdsdl_plan_scalar_unsigned__uavcan_register_List_1_0__request__0__deser(indexRaw));
  offsetBits += 16;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}

// This service allows the caller to discover the names of all registers available on the server
// by iterating the index field from zero until an empty name is returned.
//
// The ordering of the registers shall remain constant while the server is running.
// The ordering is not guaranteed to remain unchanged when the server node is restarted.
export interface List_Response {
  // Empty name in response means that the index is out of bounds, i.e., discovery is finished.
  name: Name;
}


export function serializeList_Response(value: List_Response): Uint8Array {
  const out = new Uint8Array(256);
  let offsetBits = 0;
  const mlir_llvmdsdl_plan_capacity_check__uavcan_register_List_1_0__response = (capacityBits: number): boolean => 2048 <= capacityBits;

  if (!mlir_llvmdsdl_plan_capacity_check__uavcan_register_List_1_0__response(out.length * 8)) {
    throw new Error("serialization buffer too small");
  }
  const nameAlignedOffsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  for (let nameAlignBit = offsetBits; nameAlignBit < nameAlignedOffsetBits; ++nameAlignBit) {
    dsdlRuntime.setBit(out, nameAlignBit, false);
  }
  offsetBits = nameAlignedOffsetBits;
  const nameBytes = serializeName(value.name);
  dsdlRuntime.copyBits(out, offsetBits, nameBytes, 0, 2048);
  offsetBits += 2048;
  const usedBytes = dsdlRuntime.byteLengthForBits(offsetBits);
  return out.subarray(0, usedBytes);
}

export function deserializeList_Response(bytes: Uint8Array): { value: List_Response; consumed: number } {
  const value = {} as List_Response;
  let offsetBits = 0;
  offsetBits = Math.trunc((offsetBits + 7) / 8) * 8;
  const nameBytes = dsdlRuntime.extractBits(bytes, offsetBits, 2048);
  value.name = deserializeName(nameBytes).value;
  offsetBits += 2048;
  const consumed = Math.min(bytes.length, dsdlRuntime.byteLengthForBits(offsetBits));
  return { value, consumed };
}

export type List = List_Request;
