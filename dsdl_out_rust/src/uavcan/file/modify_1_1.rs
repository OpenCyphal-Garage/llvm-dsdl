// Generated by llvmdsdl 0.2.3 (Rust backend).
// Source: uavcan.file.Modify.1.1
#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#![allow(non_upper_case_globals)]
#![allow(deprecated)]

use crate::uavcan::file::error_1_0::uavcan_file_Error;
use crate::uavcan::file::path_2_0::uavcan_file_Path;

/// Manipulate a remote file system entry. Applies to files, directories, and links alike.
/// If the remote entry is a directory, all nested entries will be affected, too.
///
/// The server should perform all operations atomically, unless atomicity is not supported by
/// the underlying file system.
///
/// Atomic copying can be effectively employed by remote nodes before reading or after writing
/// the file to minimize the possibility of race conditions.
/// For example, before reading a large file from the server, the cilent might opt to create
/// a temporary copy of it first, then read the copy, and delete it upon completion. Likewise,
/// a similar strategy can be employed for writing, where the file is first written at a
/// temporary location, and then moved to its final destination. These approaches, however,
/// may lead to creation of dangling temporary files if the client failed to dispose of them
/// properly, so that risk should be taken into account.
///
/// Move/Copy
///   Specify the source path and the destination path.
///   If the source does not exist, the operation will fail.
///   Set the preserve_source flag to copy rather than move.
///   If the destination exists and overwrite_destination is not set, the operation will fail.
///   If the target path includes non-existent directories, they will be created (like "mkdir -p").
///
/// Touch
///   Specify the destination path and make the source path empty.
///   If the path exists (file/directory/link), its modification time will be updated.
///   If the path does not exist, an empty file will be created.
///   If the target path includes non-existent directories, they will be created (like "mkdir -p").
///   Flags are ignored.
///
/// Remove
///   Specify the source path (file/directory/link) and make the destination path empty.
///   Fails if the path does not exist.
///   Flags are ignored.
#[derive(Clone, Debug, PartialEq)]
pub struct uavcan_file_Modify_Request {
    /// Do not remove the source. Used to copy instead of moving.
    pub preserve_source: bool,
    /// If the destination exists, remove it beforehand.
    pub overwrite_destination: bool,
    pub source: uavcan_file_Path,
    pub destination: uavcan_file_Path,
}

impl Default for uavcan_file_Modify_Request {
    fn default() -> Self {
        Self {
            preserve_source: false,
            overwrite_destination: false,
            source: uavcan_file_Path::default(),
            destination: uavcan_file_Path::default(),
        }
    }
}

impl uavcan_file_Modify_Request {
    pub const FULL_NAME: &'static str = "uavcan.file.Modify.Request";
    pub const IS_DEPRECATED: bool = false;
    pub const FULL_NAME_AND_VERSION: &'static str = "uavcan.file.Modify.Request.1.1";
    pub const EXTENT_BYTES: usize = 600;
    pub const SERIALIZATION_BUFFER_SIZE_BYTES: usize = 516;
    pub const ZOH_ALIAS_ELIGIBLE: bool = false;
    pub const ZOH_ALIAS_REASON: &'static str = "sub-byte-field";
    pub const __LLVMDSDL_MEMORY_MODE: crate::dsdl_runtime::DsdlMemoryMode = crate::dsdl_runtime::DsdlMemoryMode::MaxInline;
    pub const __LLVMDSDL_INLINE_THRESHOLD_BYTES: usize = 256usize;

    pub fn serialize(&self, buffer: &mut [u8]) -> core::result::Result<usize, i8> {
        let mut offset_bits: usize = 0;
        let mlir_llvmdsdl_plan_capacity_check__uavcan_file_Modify_1_1__request = |capacity_bits: i64| -> i8 {
            if 4128i64 > capacity_bits {
                -crate::dsdl_runtime::DSDL_RUNTIME_ERROR_SERIALIZATION_BUFFER_TOO_SMALL
            } else {
                crate::dsdl_runtime::DSDL_RUNTIME_SUCCESS
            }
        };
        let _err_capacity = mlir_llvmdsdl_plan_capacity_check__uavcan_file_Modify_1_1__request(buffer.len().saturating_mul(8) as i64);
        if _err_capacity != crate::dsdl_runtime::DSDL_RUNTIME_SUCCESS {
            return Err(_err_capacity);
        }
        let _err0_ = crate::dsdl_runtime::set_bit(buffer, offset_bits, self.preserve_source);
        if _err0_ < 0 { return Err(_err0_); }
        offset_bits += 1;
        let _err1_ = crate::dsdl_runtime::set_bit(buffer, offset_bits, self.overwrite_destination);
        if _err1_ < 0 { return Err(_err1_); }
        offset_bits += 1;
        let _err2_ = crate::dsdl_runtime::set_uxx(buffer, offset_bits, 0, 30u8);
        if _err2_ < 0 { return Err(_err2_); }
        offset_bits += 30;
        while (offset_bits % 8usize) != 0 {
            let _err3_ = crate::dsdl_runtime::set_bit(buffer, offset_bits, false);
            if _err3_ < 0 { return Err(_err3_); }
            offset_bits += 1;
        }
        let mut _size_bytes4_ = 256usize;
        let _start = crate::dsdl_runtime::choose_min(offset_bits / 8, buffer.len());
        let _end = _start.saturating_add(_size_bytes4_).min(buffer.len());
        let _err5_ = self.source.serialize(&mut buffer[_start.._end]);
        match _err5_ {
            Ok(v) => _size_bytes4_ = v,
            Err(e) => return Err(e),
        }
        offset_bits += _size_bytes4_ * 8;
        while (offset_bits % 8usize) != 0 {
            let _err6_ = crate::dsdl_runtime::set_bit(buffer, offset_bits, false);
            if _err6_ < 0 { return Err(_err6_); }
            offset_bits += 1;
        }
        let mut _size_bytes7_ = 256usize;
        let _start = crate::dsdl_runtime::choose_min(offset_bits / 8, buffer.len());
        let _end = _start.saturating_add(_size_bytes7_).min(buffer.len());
        let _err8_ = self.destination.serialize(&mut buffer[_start.._end]);
        match _err8_ {
            Ok(v) => _size_bytes7_ = v,
            Err(e) => return Err(e),
        }
        offset_bits += _size_bytes7_ * 8;
        while (offset_bits % 8usize) != 0 {
            let _err9_ = crate::dsdl_runtime::set_bit(buffer, offset_bits, false);
            if _err9_ < 0 { return Err(_err9_); }
            offset_bits += 1;
        }
        Ok(offset_bits / 8)
    }

    pub fn deserialize(&mut self, buffer: &[u8]) -> core::result::Result<usize, i8> {
        let capacity_bytes = buffer.len();
        let capacity_bits = capacity_bytes.saturating_mul(8);

        let mut offset_bits: usize = 0;
        self.preserve_source = crate::dsdl_runtime::get_bit(buffer, offset_bits);
        offset_bits += 1;
        self.overwrite_destination = crate::dsdl_runtime::get_bit(buffer, offset_bits);
        offset_bits += 1;
        offset_bits += 30;
        offset_bits = (offset_bits + 7) & !7usize;
        let _start = crate::dsdl_runtime::choose_min(offset_bits / 8, buffer.len());
        let _slice = &buffer[_start..buffer.len()];
        let _consumed = match self.source.deserialize(_slice) { Ok(v) => v, Err(e) => return Err(e) };

        offset_bits += _consumed * 8;
        offset_bits = (offset_bits + 7) & !7usize;
        let _start = crate::dsdl_runtime::choose_min(offset_bits / 8, buffer.len());
        let _slice = &buffer[_start..buffer.len()];
        let _consumed = match self.destination.deserialize(_slice) { Ok(v) => v, Err(e) => return Err(e) };

        offset_bits += _consumed * 8;
        offset_bits = (offset_bits + 7) & !7usize;
        Ok(crate::dsdl_runtime::choose_min(offset_bits, capacity_bits) / 8)
    }

    pub fn deserialize_with_consumed(&mut self, buffer: &[u8]) -> (i8, usize) {
        match self.deserialize(buffer) {
            Ok(consumed) => (0, consumed),
            Err(rc) => (rc, buffer.len()),
        }
    }

    pub fn try_deserialize_view<'a>(buffer: &'a [u8]) -> core::result::Result<(&'a [u8], usize), i8> {
        if !Self::ZOH_ALIAS_ELIGIBLE || cfg!(target_endian = "big") {
            return Err(-crate::dsdl_runtime::DSDL_RUNTIME_ERROR_INVALID_ARGUMENT);
        }
        let required = Self::SERIALIZATION_BUFFER_SIZE_BYTES;
        if buffer.len() < required {
            return Err(-crate::dsdl_runtime::DSDL_RUNTIME_ERROR_SERIALIZATION_BUFFER_TOO_SMALL);
        }
        Ok((&buffer[..required], required))
    }

    pub fn try_serialize_view(view_bytes: &[u8], buffer: &mut [u8]) -> core::result::Result<usize, i8> {
        if !Self::ZOH_ALIAS_ELIGIBLE || cfg!(target_endian = "big") {
            return Err(-crate::dsdl_runtime::DSDL_RUNTIME_ERROR_INVALID_ARGUMENT);
        }
        let required = Self::SERIALIZATION_BUFFER_SIZE_BYTES;
        if view_bytes.len() != required {
            return Err(-crate::dsdl_runtime::DSDL_RUNTIME_ERROR_INVALID_ARGUMENT);
        }
        if buffer.len() < required {
            return Err(-crate::dsdl_runtime::DSDL_RUNTIME_ERROR_SERIALIZATION_BUFFER_TOO_SMALL);
        }
        buffer[..required].copy_from_slice(&view_bytes[..required]);
        Ok(required)
    }

    pub fn to_bytes(&self) -> core::result::Result<crate::dsdl_runtime::DsdlVec<u8>, i8> {
        let mut buffer = crate::dsdl_runtime::DsdlVec::<u8>::with_capacity(Self::SERIALIZATION_BUFFER_SIZE_BYTES);
        buffer.resize(Self::SERIALIZATION_BUFFER_SIZE_BYTES, 0u8);
        let used = self.serialize(&mut buffer)?;
        buffer.truncate(used);
        Ok(buffer)
    }

    pub fn from_bytes(buffer: &[u8]) -> core::result::Result<(Self, usize), i8> {
        let mut out = Self::default();
        let used = out.deserialize(buffer)?;
        Ok((out, used))
    }
}


/// Manipulate a remote file system entry. Applies to files, directories, and links alike.
/// If the remote entry is a directory, all nested entries will be affected, too.
///
/// The server should perform all operations atomically, unless atomicity is not supported by
/// the underlying file system.
///
/// Atomic copying can be effectively employed by remote nodes before reading or after writing
/// the file to minimize the possibility of race conditions.
/// For example, before reading a large file from the server, the cilent might opt to create
/// a temporary copy of it first, then read the copy, and delete it upon completion. Likewise,
/// a similar strategy can be employed for writing, where the file is first written at a
/// temporary location, and then moved to its final destination. These approaches, however,
/// may lead to creation of dangling temporary files if the client failed to dispose of them
/// properly, so that risk should be taken into account.
///
/// Move/Copy
///   Specify the source path and the destination path.
///   If the source does not exist, the operation will fail.
///   Set the preserve_source flag to copy rather than move.
///   If the destination exists and overwrite_destination is not set, the operation will fail.
///   If the target path includes non-existent directories, they will be created (like "mkdir -p").
///
/// Touch
///   Specify the destination path and make the source path empty.
///   If the path exists (file/directory/link), its modification time will be updated.
///   If the path does not exist, an empty file will be created.
///   If the target path includes non-existent directories, they will be created (like "mkdir -p").
///   Flags are ignored.
///
/// Remove
///   Specify the source path (file/directory/link) and make the destination path empty.
///   Fails if the path does not exist.
///   Flags are ignored.
#[derive(Clone, Debug, PartialEq)]
pub struct uavcan_file_Modify_Response {
    pub error: uavcan_file_Error,
}

impl Default for uavcan_file_Modify_Response {
    fn default() -> Self {
        Self {
            error: uavcan_file_Error::default(),
        }
    }
}

impl uavcan_file_Modify_Response {
    pub const FULL_NAME: &'static str = "uavcan.file.Modify.Response";
    pub const IS_DEPRECATED: bool = false;
    pub const FULL_NAME_AND_VERSION: &'static str = "uavcan.file.Modify.Response.1.1";
    pub const EXTENT_BYTES: usize = 48;
    pub const SERIALIZATION_BUFFER_SIZE_BYTES: usize = 2;
    pub const ZOH_ALIAS_ELIGIBLE: bool = false;
    pub const ZOH_ALIAS_REASON: &'static str = "invalid-bit-length";
    pub const __LLVMDSDL_MEMORY_MODE: crate::dsdl_runtime::DsdlMemoryMode = crate::dsdl_runtime::DsdlMemoryMode::MaxInline;
    pub const __LLVMDSDL_INLINE_THRESHOLD_BYTES: usize = 256usize;

    pub fn serialize(&self, buffer: &mut [u8]) -> core::result::Result<usize, i8> {
        let mut offset_bits: usize = 0;
        let mlir_llvmdsdl_plan_capacity_check__uavcan_file_Modify_1_1__response = |capacity_bits: i64| -> i8 {
            if 16i64 > capacity_bits {
                -crate::dsdl_runtime::DSDL_RUNTIME_ERROR_SERIALIZATION_BUFFER_TOO_SMALL
            } else {
                crate::dsdl_runtime::DSDL_RUNTIME_SUCCESS
            }
        };
        let _err_capacity = mlir_llvmdsdl_plan_capacity_check__uavcan_file_Modify_1_1__response(buffer.len().saturating_mul(8) as i64);
        if _err_capacity != crate::dsdl_runtime::DSDL_RUNTIME_SUCCESS {
            return Err(_err_capacity);
        }
        while (offset_bits % 8usize) != 0 {
            let _err0_ = crate::dsdl_runtime::set_bit(buffer, offset_bits, false);
            if _err0_ < 0 { return Err(_err0_); }
            offset_bits += 1;
        }
        let mut _size_bytes1_ = 2usize;
        let _start = crate::dsdl_runtime::choose_min(offset_bits / 8, buffer.len());
        let _end = _start.saturating_add(_size_bytes1_).min(buffer.len());
        let _err2_ = self.error.serialize(&mut buffer[_start.._end]);
        match _err2_ {
            Ok(v) => _size_bytes1_ = v,
            Err(e) => return Err(e),
        }
        offset_bits += _size_bytes1_ * 8;
        while (offset_bits % 8usize) != 0 {
            let _err3_ = crate::dsdl_runtime::set_bit(buffer, offset_bits, false);
            if _err3_ < 0 { return Err(_err3_); }
            offset_bits += 1;
        }
        Ok(offset_bits / 8)
    }

    pub fn deserialize(&mut self, buffer: &[u8]) -> core::result::Result<usize, i8> {
        let capacity_bytes = buffer.len();
        let capacity_bits = capacity_bytes.saturating_mul(8);

        let mut offset_bits: usize = 0;
        offset_bits = (offset_bits + 7) & !7usize;
        let _start = crate::dsdl_runtime::choose_min(offset_bits / 8, buffer.len());
        let _slice = &buffer[_start..buffer.len()];
        let _consumed = match self.error.deserialize(_slice) { Ok(v) => v, Err(e) => return Err(e) };

        offset_bits += _consumed * 8;
        offset_bits = (offset_bits + 7) & !7usize;
        Ok(crate::dsdl_runtime::choose_min(offset_bits, capacity_bits) / 8)
    }

    pub fn deserialize_with_consumed(&mut self, buffer: &[u8]) -> (i8, usize) {
        match self.deserialize(buffer) {
            Ok(consumed) => (0, consumed),
            Err(rc) => (rc, buffer.len()),
        }
    }

    pub fn try_deserialize_view<'a>(buffer: &'a [u8]) -> core::result::Result<(&'a [u8], usize), i8> {
        if !Self::ZOH_ALIAS_ELIGIBLE || cfg!(target_endian = "big") {
            return Err(-crate::dsdl_runtime::DSDL_RUNTIME_ERROR_INVALID_ARGUMENT);
        }
        let required = Self::SERIALIZATION_BUFFER_SIZE_BYTES;
        if buffer.len() < required {
            return Err(-crate::dsdl_runtime::DSDL_RUNTIME_ERROR_SERIALIZATION_BUFFER_TOO_SMALL);
        }
        Ok((&buffer[..required], required))
    }

    pub fn try_serialize_view(view_bytes: &[u8], buffer: &mut [u8]) -> core::result::Result<usize, i8> {
        if !Self::ZOH_ALIAS_ELIGIBLE || cfg!(target_endian = "big") {
            return Err(-crate::dsdl_runtime::DSDL_RUNTIME_ERROR_INVALID_ARGUMENT);
        }
        let required = Self::SERIALIZATION_BUFFER_SIZE_BYTES;
        if view_bytes.len() != required {
            return Err(-crate::dsdl_runtime::DSDL_RUNTIME_ERROR_INVALID_ARGUMENT);
        }
        if buffer.len() < required {
            return Err(-crate::dsdl_runtime::DSDL_RUNTIME_ERROR_SERIALIZATION_BUFFER_TOO_SMALL);
        }
        buffer[..required].copy_from_slice(&view_bytes[..required]);
        Ok(required)
    }

    pub fn to_bytes(&self) -> core::result::Result<crate::dsdl_runtime::DsdlVec<u8>, i8> {
        let mut buffer = crate::dsdl_runtime::DsdlVec::<u8>::with_capacity(Self::SERIALIZATION_BUFFER_SIZE_BYTES);
        buffer.resize(Self::SERIALIZATION_BUFFER_SIZE_BYTES, 0u8);
        let used = self.serialize(&mut buffer)?;
        buffer.truncate(used);
        Ok(buffer)
    }

    pub fn from_bytes(buffer: &[u8]) -> core::result::Result<(Self, usize), i8> {
        let mut out = Self::default();
        let used = out.deserialize(buffer)?;
        Ok((out, used))
    }
}


pub type uavcan_file_Modify = uavcan_file_Modify_Request;
