mkdocs==1.6.1
mkdocs-material==9.5.50
pymdown-extensions==10.14.3
# Pygments arrives as a transitive dependency and is pinned deliberately: 2.20
# escapes the highlighter's `filename` option unconditionally, while
# pymdown-extensions 10.14.3 passes it as None, which fails every page holding a
# fenced code block. Raising pymdown-extensions instead is not open --
# mkdocs-material 9.5.50 constrains it to ~=10.2 -- so the ceiling belongs here
# until that pair moves together.
pygments==2.19.2
